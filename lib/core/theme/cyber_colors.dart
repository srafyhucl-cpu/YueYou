import 'package:flutter/material.dart';

class CyberColors {
  // 极致的深色背景（带一点极暗的蓝灰调，比纯黑更高级，让霓虹色更凸显）
  static const Color background = Color(0xFF0D0E15);

  // 赛博/黑客帝国经典的荧光绿（主色调，用于光标和已读文字）
  static const Color neonGreen = Color(0xFF00FF41);

  // 赛博朋克必不可少的霓虹粉（用于 2048 的高分方块或特殊警告）
  static const Color neonPink = Color(0xFFFE019A);

  // 提词器未读文字的暗色（制造对比度）
  static const Color textDim = Color(0xFF4A5568);

  // 柔和的发光阴影色（带透明度）
  static const Color glowShadow = Color(0x8800FF41);
}
