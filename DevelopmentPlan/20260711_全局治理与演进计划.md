# 20260711 全局治理与演进计划

## 1. 计划定位

本计划基于 2026-07-11 全仓复盘结果，作为阅游下一阶段唯一执行基线。执行顺序固定为：

1. 安全与隐私止血。
2. 恢复 CI 和发布可信度。
3. 收敛架构边界。
4. 验证听读产品主线。
5. 仅在通过决策门后启动 AI 试点。

任何新功能不得绕过前置里程碑。P0 未清零前，暂停 RAG、角色聊天、内容生成和大规模 UI 改造。

## 2. 当前基线

| 维度 | 当前结果 | 判断 |
| --- | --- | --- |
| Flutter 静态分析 | `flutter analyze` 零问题 | 通过 |
| Flutter 全量测试 | 672 通过、4 跳过、0 失败 | 通过，但存在跳过项 |
| 总体行覆盖率 | 79.43% | 未达到发布目标 |
| CI 业务核心覆盖率 | 82.21% | 未达到 90% 门槛 |
| AI 工程门禁 | 0 阻断、22 警告 | 与零警告目标不一致 |
| Go 服务端 | `go vet`、`go build` 通过 | 可编译 |
| Go 服务端测试 | `[no test files]` | 阻断生产发布 |
| Android 正式签名 | 密码和本机路径进入版本库 | P0 安全事件 |
| 隐私前置 | Sentry、更新检查和部分 Provider 可先于同意初始化 | P0 合规缺口 |
| TTS 服务 | 无鉴权、限流、请求体限制，日志记录朗读原文 | P0 隐私与滥用风险 |
| 后端权威 | 已跟踪 Go 与未跟踪 Python 迁移并存 | 决策未收敛 |
| 发布分支 | 默认分支为 `yueyou_test`，无 `v1.1.0` Tag | 发布治理不完整 |

## 3. 总体目标与完成定义

### 3.1 目标

- 形成可公开发布、可重复构建、可审计的 Android 交付链。
- 确保用户同意隐私政策前不存在任何外部网络请求或第三方 SDK 初始化。
- 确保 TTS 文本、音频、日志和缓存具有明确的数据最小化与生命周期策略。
- 让 CI 的绿色状态真实代表分析、测试、覆盖率、服务端和 Release 构建全部通过。
- 保持听读为产品主线，2048 作为可选陪伴模式，避免继续横向堆叠功能。

### 3.2 全局完成定义

- [ ] 所有 P0 项关闭并有自动化回归证据。
- [x] `flutter analyze` 零问题，测试目录不再绕过必要 lint。
- [x] `dart scripts/ai_code_checker.dart` 为 0 阻断、0 警告。
- [x] `flutter test --coverage --concurrency=1` 全量通过。
- [x] CI 业务核心覆盖率不低于 90%，总体覆盖率不低于 80%。
- [x] `go test ./...`、`go vet ./...`、`go build ./...` 全部通过。
- [x] arm64 Release APK 使用外部签名配置成功构建，仓库内无签名密码和私钥。
- [x] 首次启动拒绝隐私协议时，网络请求计数严格为 0。
- [x] 生产 TTS 不记录原文，音频不可公开永久访问。
- [ ] 发布分支、版本号、Tag、变更记录和 APK 版本一致。

## 4. 里程碑总览

| 里程碑 | 目标时间 | 核心产出 | 进入条件 | 退出条件 |
| --- | --- | --- | --- | --- |
| M0 安全止血 | 2026-07-11 至 2026-07-13 | 签名、隐私、TTS 安全闭环 | 本计划确认 | P0 全部关闭 |
| M1 交付恢复 | 2026-07-14 至 2026-07-18 | CI、覆盖率、后端测试、Release 构建 | M0 完成 | CI 全绿且可重复构建 |
| M2 架构收敛 | 2026-07-19 至 2026-08-01 | composition 层、模块解耦、单后端权威 | M1 完成 | 依赖边界门禁通过 |
| M3 产品验证 | 2026-08-02 至 2026-08-22 | 听读核心体验与真实用户验证 | M2 完成 | 产品决策门通过 |
| M4 AI 试点 | 2026-08-23 起 | 带引用的章节问答 MVP | M3 决策门通过 | AI 指标达标或明确停止 |

时间为目标窗口，不允许通过降低门禁换取按期完成。

## 5. M0：安全与隐私止血

### PR-1 Android 签名与构建配置治理

**目标**：清除已泄露签名信息，恢复跨机器可重复构建能力。

**实施项**：

- [x] 确认应用是否已在任何应用市场发布。
- [x] 未发布：废弃旧 JKS，生成新签名文件和全新密码。
- [ ] 已发布：按应用市场签名规则处理密钥轮换或密码变更，禁止直接替换导致升级链断裂。
- [x] 只保留一套 Android Gradle DSL，建议保留 `build.gradle.kts`。
- [x] 签名参数仅从未跟踪的 `android/key.properties` 或 CI Secret 读取。
- [x] Release 缺少正式签名时必须失败，禁止回退 debug 签名。
- [x] 开启 `minifyEnabled` 和 `shrinkResources`，验证 TTS、Rive、Sentry 等插件不受混淆影响。
- [x] 经用户明确确认后，使用历史重写工具移除 Git 历史中的密码；该步骤涉及远端强制更新，不得自动执行。

**验收**：

- `rg -n "storePassword|keyPassword|D:/" android/app` 不命中真实凭据或本机路径。
- 无 `key.properties`、JKS、Keystore 被 Git 跟踪。
- 使用本地外部配置成功构建 arm64 Release APK。
- CI 不持有明文密码，日志不回显任何签名参数。

**2026-07-11 执行记录**：

- 已删除旧 Groovy Gradle 入口：`android/app/build.gradle`、`android/build.gradle`、
  `android/settings.gradle`，避免双 DSL 并存和旧文件继续暴露明文签名。
- `android/app/build.gradle.kts` 改为从 `android/key.properties` 或
  `ANDROID_STORE_FILE`、`ANDROID_STORE_PASSWORD`、`ANDROID_KEY_ALIAS`、
  `ANDROID_KEY_PASSWORD` 读取正式签名；Release/Bundle 任务缺配置时直接失败。
- `android/gradle.properties` 移除本机 `org.gradle.java.home` 路径。
- `android/key.properties.template` 改为纯占位模板，不再记录具体 JKS 文件名、
  alias 或密码线索。
- 验证结果：`flutter analyze` 零问题；`flutter test --concurrency=1` 全量通过
  （672 passed、4 skipped）；`dart scripts/ai_code_checker.dart` 为 0 阻断、
  22 条存量 warning。
- Release 构建验证已完成：`flutter build apk --release --target-platform android-arm64
  --split-per-abi --dart-define=TTS_SERVER_URL=https://hclstudio.cn/api/v1/tts
  --dart-define=BOOK_API_BASE=https://hclstudio.cn/api/v1` 成功生成
  `build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`，大小 25,852,724 字节，
  SHA-256 为 `3DAE195D7926341132143F7E3797E4CC2A16FA351A247DB9375043749F425FDD`。
- 用户已确认：应用尚未上架；旧 JKS 可废弃重建；允许后续执行 Git 历史重写。
- 已删除旧本机 `android/yueyou-release.jks` 与 `android/key.properties`，重新生成
  未跟踪的 `android/yueyou-release.jks` 和 `android/key.properties`，新别名为
  `yueyou_release_v2`；`keytool -list` 可读取 `PrivateKeyEntry`。
- 已安装可用 JDK 17 到 `D:\Work\Java\jdk-17.0.19+10`，下载包已删除；使用
  `D:\Work\GradleCache` 作为 `GRADLE_USER_HOME`，并把 Gradle wrapper 升级到
  `gradle-8.13-bin.zip` 后，`:app:validateSigningRelease` 通过。
- 为恢复 Release 构建，Kotlin DSL 补回 Maven 镜像、第三方插件 Kotlin/JVM
  目标对齐，以及 Flutter optional Play Core R8 `dontwarn` 规则。
- 历史重写已在独立镜像仓库完成，并强推 `master`、`yueyou_old_test`、
  `yueyou_test` 三个远端分支；新鲜 clone 验证旧密码和旧 JKS 绝对路径不再命中。

### PR-2 隐私同意前置启动闸门

**目标**：把隐私弹窗从 UI 覆盖层升级为真正的基础设施启动闸门。

**设计**：

```text
进程启动
  -> 仅初始化 Flutter Binding 和本地同意状态
  -> 未同意：ConsentApp，不创建业务 Provider，不初始化第三方 SDK
  -> 同意：持久化同意状态
  -> 初始化 Sentry、音频、业务 ProviderScope 和 YueYouApp
  -> 已同意：直接进入完整初始化
```

**实施项**：

- [x] 将 Sentry 初始化移动到同意之后。
- [x] 将 `ProviderScope`、Dashboard、TTS、默认书恢复、更新检查移动到同意之后。
- [x] 同意页只允许读取本地同意状态和打开隐私政策链接。
- [x] 拒绝后退出，不创建任何业务服务。
- [x] 提供撤回同意入口；撤回后清理 Sentry 会话并在下次启动重新进入 ConsentApp。
- [x] 补首次启动、已同意启动、拒绝、撤回、升级后重新确认五条测试路径。

**验收**：

- 首次启动未点击同意前，Mock HTTP、Sentry、OSS、TTS 调用次数均为 0。
- 同意后初始化只执行一次，不出现重复 Provider 或重复播放器。
- 拒绝和撤回路径无后台残留任务。

**2026-07-12 执行记录**：

- `lib/main.dart` 新增 `YueYouStartup` 与 `ConsentApp`：未同意时只渲染同意页，不创建
  `ProviderScope`、`DashboardScreen`、Sentry、音效或环境音服务；同意后再进入完整
  初始化链路。
- `CyberLogger` 新增 `closeSentrySession()`，设置页新增“撤回隐私授权”入口；撤回时停止
  TTS、释放环境音、关闭 Sentry 会话、写回本地同意状态并退出应用。
- 新增 `test/app_startup_test.dart`，覆盖首次启动未同意、同意前无业务树、点击同意、
  已同意直启、拒绝退出和协议版本升级后重新确认 6 条启动路径；设置页测试补充隐私合规
  分组与撤回入口渲染。
- 新增 `AppInfoConfig.privacyPolicyVersion` 与本地已同意版本持久化；协议版本不一致时不
  初始化业务栈，用户重新同意后才写入当前版本。
- 验证结果：`flutter analyze` 零问题；`dart scripts/ai_code_checker.dart` 为 0 阻断、
  0 条 warning；相关 Flutter 测试 119 passed、4 skipped，`dart analyze test` 零问题。
- 遗留：撤回点击后的真实平台退出路径仍需设备或集成测试确认。

- 启动测试新增 `HttpOverrides` 守卫：首次未同意路径一旦创建 HTTP 客户端立即失败；6 条启动
  测试、`flutter analyze` 和 `dart analyze test` 均通过。该证据覆盖 Dart HTTP 能力，真实设备
  上第三方 SDK/原生网络观测仍保留为发布验收项。

### PR-3 TTS 数据安全与服务端防滥用

**目标**：消除原文日志、公开永久音频和匿名无限调用风险。

**实施项**：

- [x] 删除当前生产权威 Go 服务端朗读原文日志；未跟踪 Python 服务不进入生产权威。
- [x] 日志只记录不可逆请求 ID、字符数、音色、耗时、状态码和错误分类。
- [x] TTS 对象统一写入私有 Bucket。
- [x] 对象键改为服务端密钥参与的 HMAC-SHA256 或随机 ID，禁止裸 MD5 文本指纹。
- [x] 返回有效期不超过 10 分钟的签名下载 URL。
- [x] OSS 生命周期规则自动删除 24 小时以上的临时 TTS 音频。
- [x] 单请求正文上限保持 2000 字，同时增加 HTTP 请求体字节上限。
- [x] 增加 IP 和匿名安装 ID 双维度限流、全局并发上限和有界等待队列。
- [x] 请求取消后必须退出信号量等待或合成流程。
- [x] 增加 `/health` 和 `/ready`，分别表示进程存活与 OSS/TTS 依赖可用。
- [x] 更新隐私政策，明确处理目的、第三方、音频保留期限和删除方式。

**建议初始阈值**：

| 项目 | 初始值 | 调整依据 |
| --- | --- | --- |
| 单 IP 请求 | 30 次/分钟 | 生产监控与误伤率 |
| 单安装 ID 请求 | 120 次/小时 | 连续听读的句子吞吐 |
| 全局合成并发 | 1 | edge-tts 当前约束 |
| 等待队列 | 50 | 防止无界 goroutine/协程 |
| 请求体上限 | 16KB | 2000 中文字符及 JSON 余量 |
| 签名 URL 有效期 | 10 分钟 | 下载重试需求 |
| OSS 临时对象生命周期 | 24 小时 | 本地缓存已承担长期复用 |

**验收**：

- 日志、Sentry 和错误响应均不包含用户原文。
- 未签名 URL 无法访问音频；过期 URL 返回拒绝。
- 超限请求返回 `429`，队列满时快速失败。
- 并发、取消、超时、OSS 失败和签名过期均有自动化测试。

**2026-07-12 执行记录**：

- `server/handler_tts.go` 移除明文原文日志与裸 MD5 对象键；TTS 临时对象键改为
  `HMAC-SHA256(voice + text)`，对象路径为 `cache/tts/v2/*.mp3`。
- 上传 TTS 音频时显式使用 `oss.ObjectACL(oss.ACLPrivate)`，返回 URL 改为
  OSS `SignURL` 生成的 10 分钟短效下载链接；生产 Bucket 级私有 ACL 与 24 小时
  生命周期规则已通过 OSS 原生接口配置并回读确认。
- 服务端新增 16KB HTTP 请求体上限、2000 字符正文上限、IP 每分钟 30 次和匿名安装
  ID 每小时 120 次限流、全局 edge-tts 并发 1、等待队列 50，以及取消等待退出逻辑。
- 客户端新增本地随机匿名安装 ID，并在 TTS POST Header 中发送
  `X-YueYou-Install-ID`；该 ID 仅用于服务端限流，不包含阅读内容或设备信息。
- 新增 `/health` 和 `/ready`；`/ready` 同时检查 OSS 存储与 `edge-tts` 执行器。
- `server/handler_privacy.go` 更新 TTS 处理目的、日志脱敏、签名 URL 有效期、
  临时音频保留目标和第三方服务说明。
- 自动化覆盖：`server/handler_tts_test.go` 验证无原文日志、签名 URL、请求体上限、
  IP 限流、队列满快速失败、缓存命中跳过合成、`/health`、`/ready`；Flutter 侧
  验证匿名安装 ID 本地稳定生成并随 TTS POST 发出。
- 自动化补充：`server/handler_tts_test.go` 已覆盖签名 TTL 参数、等待队列取消和同键合成等待
  取消；上传失败、签名失败和合成取消路径已有回归测试。剩余仅为真实 OSS 过期 URL 访问拒绝的
  外部集成验证；Bucket 私有策略与 24 小时生命周期已完成生产配置并回读验证。

### PR-4 Android 本地数据备份边界

**目标**：保证“纯本地数据”不因系统自动备份产生未声明云同步。

**实施项**：

- [x] 明确选择完全禁用备份，或通过 `dataExtractionRules` 排除书籍正文、进度、设置和 TTS 缓存。
- [x] 默认采用完全禁用备份；未来需要跨设备恢复时再单独立项并更新隐私政策。
- [x] 评估本地书籍正文是否需要应用级加密；至少避免进入系统日志、崩溃附件和备份。

**验收**：

- Android Manifest 和备份规则有自动化静态检查。
- 隐私政策、本地存储实现和系统备份行为一致。

**2026-07-12 执行记录**：

- `android/app/src/main/AndroidManifest.xml` 显式设置 `android:allowBackup="false"`、
  `android:fullBackupContent="false"` 和 `android:dataExtractionRules="@xml/data_extraction_rules"`。
- 新增 `android/app/src/main/res/xml/data_extraction_rules.xml`，同时排除
  `cloud-backup` 与 `device-transfer` 下的本地文件、数据库、偏好设置和外部文件域。
- `scripts/ai_checks/rules.dart` 新增 `AndroidBackupRule`，要求 Manifest 禁用自动备份、
  禁用旧版完整备份，并校验 Android 12+ 数据提取规则文件存在且覆盖云备份与设备迁移。
- `test/scripts/ai_code_checker_test.dart` 新增 Manifest 缺少 `allowBackup=false` 与
  `dataExtractionRules` 引用文件缺失的阻断回归测试。
- `server/handler_privacy.go`、应用内隐私摘要和 README 同步说明：本地数据不进入
  Android 系统自动备份或设备迁移；未来跨设备恢复需另行立项并征得明确同意。
- 本轮评估：当前导入书籍正文、阅读进度、设置与 TTS 缓存仍保留本地明文可用性模型；
  上线前先通过禁用系统备份、日志脱敏和崩溃附件排除阻断隐式外传。应用级加密不并入
  PR-4，后续如支持敏感书库或云同步再单独评估密钥管理、恢复和性能成本。

## 6. M1：恢复交付可信度

### PR-5 CI 与覆盖率闭环

**实施项**：

- [x] 补齐启动隐私、`tts_http_client`、更新 HTTP、音频适配器和异常分支测试。
- [x] 保持业务核心覆盖率计算口径稳定，不通过扩大排除范围过门禁。
- [x] 将 AI 门禁 21 条视觉硬编码警告清零。
- [x] 对 `tts_engine_service.dart` 执行职责评估；仅在可独立测试、边界更清晰且无生命周期破坏时拆分。
- [x] 清理测试目录 lint 后，移除 `test/**` analyzer 排除，或增加独立 `dart analyze test` 步骤。
- [x] 分阶段启用 `strict-inference`、`strict-casts`、`strict-raw-types`，每项独立 PR。
- [x] AI 门禁警告清零后，将 warning 设为 CI 失败。

**验收**：

- 业务核心覆盖率不低于 90%，总体覆盖率不低于 80%。
- Flutter 分析、测试、AI 门禁和覆盖率步骤任一失败均阻断合并。
- 4 个跳过测试逐项登记原因；能替代的全部取消跳过。

**2026-07-12 执行记录**：

- `lib/core/theme/cyber_dimensions.dart` 新增阴影模糊与波形柱间距 token，替换音频控制台、
  仪表盘、2048、导入按钮、设置页和通用弹窗中的硬编码 `blurRadius`、`EdgeInsets` 数值。
- 职责评估结论：`tts_engine_service.dart` 中的物理进度监听、`playFile`、暂停/恢复/停止与
  播放完成信号属于底层音频播放控制，可被独立 mock 测试，抽出后依赖方向仍为
  service -> controller -> domain interfaces，不引入循环依赖。
- 新增 `lib/features/audio/services/tts_playback_controller.dart`，承接本地音频文件播放、
  `onPositionChanged` / `onDurationChanged` 监听、`Completer` 播放完成信号和暂停恢复控制；
  `TtsEngineService` 保留状态同步、设置联动、诊断、下载与 Riverpod 生命周期薄壳。
- `tts_engine_service.dart` 行数从 698 降至 594；新增播放控制器 145 行，均低于对应
  service 警戒线，且未使用 `part` / `part of`。
- `dart scripts/ai_code_checker.dart` 已达到 0 阻断、0 warning。
- 已验证：`flutter analyze` 零问题；`flutter test test\features\audio test\features\reader
  --concurrency=1` 通过（268 passed）；相关 UI 与 AI 门禁测试通过（30 passed）；
  `flutter test --concurrency=1` 全量通过（680 passed、4 skipped）。
- 覆盖率闭环推进：`test/features/library/bookshelf_provider_test.dart` 补充书架 loading 状态、
  正文读取、默认书去重、粘性位短路和测试注入路径；对书架、阅读器、TTS 编排层中无法稳定
  人工触发的防御性日志兜底块标记 coverage ignore，覆盖率口径不新增业务文件排除。
- `scripts/check_coverage_gate.py` 移除成功输出中的 emoji，修复 Windows GBK 控制台下
  “覆盖率已达标但脚本因 UnicodeEncodeError 退出 1”的门禁误报。
- 覆盖率验证：`flutter test --coverage --concurrency=1` 全量通过（684 passed、4 skipped）；
  `python scripts\check_coverage_gate.py --overall 80 --core 90` 通过，总体行覆盖率 80.77%，
  核心文件均不低于 90%（`tts_audio_notifier.dart` 90.18%、`reader_provider.dart` 90.67%、
  `bookshelf_provider.dart` 98.46%）。
- 4 个 skip 均来自 `test/core/i_storage_service_test.dart` 的 LocalStorageService 通用正文契约组；
  同文件下方已有 `LocalStorageService - 书籍正文（需路径 mock）` 专属 group 覆盖正文保存与删除。
- PR-5 验收补充：启动隐私、`tts_http_client`、更新 HTTP、音频适配器和异常分支测试均已在
  `test/app_startup_test.dart`、`test/features/audio/*`、`test/features/update/update_service_test.dart`
  等用例中覆盖；4 个 skip 已登记且有 `path_provider` mock 专属 group 替代覆盖。
- CI 门禁收紧：`.github/workflows/flutter-ci.yml` 新增 `dart analyze test` 独立步骤，覆盖
  `analysis_options.yaml` 中 `test/**` 排除后的测试目录 lint；新增
  `python scripts/check_coverage_gate.py --overall 80 --core 90` 步骤，使 GitHub Actions 与本地
  总体 80% + 核心文件 90% 门槛一致。
- `scripts/ai_code_checker.dart` 将失败条件从“仅 blocking”提升为“blocking 或 warning”，并通过
  `test/scripts/ai_code_checker_test.dart` 固化“只有 warning 也必须失败”的 CLI 退出策略。
- 验证结果：`dart analyze test` 零问题；`flutter analyze` 零问题；
  `dart scripts\ai_code_checker.dart` 为 0 阻断、0 warning；`flutter test --concurrency=1`
  全量通过（685 passed、4 skipped）。
- strict lint 第一阶段：`analysis_options.yaml` 已启用 `strict-inference: true`；补齐
  `showCyberModal<void>`、`showDialog<void>`、`Future<void>.delayed` 和 `catchError((Object e) ...)`
  等显式类型，不改变运行时行为。
- 验证结果：`flutter analyze` 零问题；`dart analyze test` 零问题；相关音频、阅读、仪表盘、
  设置测试通过（283 passed）。
- strict lint 第二阶段：`analysis_options.yaml` 已启用 `strict-casts: true`；2048 存档恢复对
  `jsonDecode` 结果先做 `Iterable` 收窄，TTS 检测弹窗步骤名称显式读取为 `String`。`strict-raw-types`
  仍保留为后续独立切片。
- 验证结果：`flutter analyze` 零问题；`dart analyze test` 零问题；相关 2048 与设置测试通过
  （97 passed）；AI 工程门禁 0 阻断、0 warning；全量测试通过（685 passed、4 skipped）。
- strict lint 第三阶段：`analysis_options.yaml` 已启用 `strict-raw-types: true`；主工程与测试目录
  均未出现新增裸类型问题，保持代码零改动。
- 验证结果：`flutter analyze` 零问题；`dart analyze test` 零问题；AI 工程门禁 0 阻断、0 warning；
  全量测试通过（685 passed、4 skipped）。

### PR-6 Go 服务端测试与契约门禁

**后端决策**：M0-M2 期间以已跟踪 Go 服务端为唯一生产权威。`server_py/` 暂不进入生产；AI 阶段如需 Python，作为独立 AI 服务接入，不以“简历技术栈”为理由替换稳定接口。

**实施项**：

- [x] 将 OSS、TTS 执行器、时钟和限流存储抽象为可注入接口。
- [x] 使用 `httptest` 覆盖 TTS、书籍、隐私、health、ready 契约。
- [x] 覆盖非法 JSON、空文本、超长文本、非法音色、限流、队列满、取消、OSS 失败。
- [x] CI 增加 `go test -race ./...`。
- [x] 增加 Flutter 客户端与 Go 服务端共享的 JSON 契约样例。

**验收**：

- `go test -race ./...`、`go vet ./...`、`go build ./...` 全部通过。
- TTS 两步下载契约有客户端和服务端双侧回归测试。

**2026-07-12 执行记录**：

- `server/handler_tts_test.go` 扩展 httptest 契约覆盖：TTS 非法 JSON、空文本、超长文本、
  非法音色、安装 ID 过长、OSS 上传失败、签名失败、存储未就绪、书籍目录、章节派发、
  章节非法请求、隐私页、health 和 ready。
- `.github/workflows/flutter-ci.yml` 的 Go 步骤新增 `go test ./...` 与 `go test -race ./...`，
  再执行 `go vet ./...` 和 `go build ./...`。
- 本地验证：`go test ./...`、`go vet ./...`、`go build ./...` 通过；`go test -race ./...`
  因本机未安装 `gcc` 无法启用 CGO，留待 Ubuntu CI 执行。
- 限流器抽出 `ttsClock` 接口，`newTTSRateLimiterWithClock` 支持测试注入；补充
  `TestTTSRateLimiterResetsWithInjectedClock` 覆盖同窗口阻断与窗口过期恢复。
- 验证结果：`go test ./...`、`go vet ./...`、`go build ./...` 通过。
- 新增 `docs/contracts/*.json` 共享契约样例，覆盖 TTS 成功/错误、书籍章节 URL、书籍目录；
  `server/contract_samples_test.go` 与 `test/contracts/api_contract_samples_test.dart` 均读取同一批样例。
- 验证结果：`go test ./...` 通过；`flutter test test\contracts\api_contract_samples_test.dart --concurrency=1`
  通过（4/4）；`dart analyze test` 零问题。
- `TestTTSHandlerTreatsClientCanceledAsSilentExit` 覆盖 TTS 合成阶段客户端取消分支，确保不写入
  OSS、不生成签名 URL、不返回错误体。
- 验证结果：`go test ./...` 通过。
- 限流器新增 `ttsRateLimitStore` 接口与默认内存实现，`newTTSRateLimiterWithClockAndStore`
  支持测试注入限流存储；补充 `TestTTSRateLimiterUsesInjectedStore`。
- 验证结果：`go test ./...`、`go vet ./...`、`go build ./...` 通过。

### PR-7 Release 构建与发布治理

**实施项**：

- [x] CI 增加 arm64 Release APK 构建，但仅受保护发布任务使用正式签名。
- [ ] 从当前 `yueyou_test` 建立正式 `main`，设置分支保护和必需检查。
- [x] 保留 `develop` 或短期 feature 分支，不再把 test 分支作为生产默认分支。
- [x] 统一 `pubspec.yaml`、Sentry release、APK versionName/versionCode 和 Git Tag。
- [x] 首个治理完成版本建议定为 `v1.1.1`，不覆盖既有 `v1.1.0` 语义。
- [ ] 发布前生成 SHA-256、变更说明和回滚说明。

**验收**：

- 新环境按 README 可从零构建。
- Tag、提交、APK 哈希和版本号一一对应。
- GitHub Actions 历史中存在完整成功的发布流水线。

**2026-07-12 执行记录**：

- `.github/workflows/flutter-ci.yml` 新增 `workflow_dispatch` 手动发布任务 `release-apk`。
- 发布任务通过 `ANDROID_KEYSTORE_BASE64` 生成临时 keystore，并使用
  `ANDROID_STORE_PASSWORD`、`ANDROID_KEY_ALIAS`、`ANDROID_KEY_PASSWORD` 注入签名参数。
- 发布任务只执行 arm64 Release APK 构建，并上传 `app-arm64-v8a-release.apk` 与 SHA-256。
- `README.md` 同步手动发布任务、签名 Secret 和 APK/SHA-256 artifact 说明。
- 已从当前 `yueyou_test` 推出远端 `main` 与 `develop`，二者均已同步至当前治理修复提交
  `6272a7795972f72b8964abc7fafb7f45181fd316`。
- 当前 GitHub 远端 HEAD 仍为 `yueyou_test`；本机无 `gh` 且无 GitHub Token，默认分支切换与
  分支保护规则仍需在 GitHub 仓库设置中完成。
- `pubspec.yaml` 版本升级为 `1.1.1+3`，`CyberLogger` Sentry release 默认值同步为
  `yueyou@1.1.1`，README 展示版本同步为 `v1.1.1`。
- `release-apk` job 从 `pubspec.yaml` 解析 version，并显式传入 `--build-name`、`--build-number`
  与 `--dart-define=APP_VERSION`，使 APK versionName/versionCode 与 Sentry release 同源。
- 验证结果：`flutter analyze` 零问题；`flutter test test\core\cyber_logger_test.dart --concurrency=1`
  通过（26 passed）；AI 工程门禁 0 阻断、0 warning；workflow YAML 可解析。
- 本提交完成后创建并推送 `v1.1.1` Git Tag，使 Tag、pubspec 版本和 Sentry release 保持一致；
  后续治理修复提交不重写既有 Tag。
- 新增 `docs/releases/v1.1.1.md`，记录版本信息、变更说明、回滚说明、本地正式签名 APK SHA-256
  和外部依赖；GitHub Actions artifact SHA-256 仍待 production workflow 生成后回填。

**2026-07-12 全局状态复核**：

- 本地全量门禁复核通过：`flutter test --coverage --concurrency=1` 为 `697 passed、4 skipped`，
  总体覆盖率 `80.65%`，核心文件均不低于 `90%`；Flutter 分析、测试目录分析、AI 门禁、Go
  test/vet/build 均通过。
- `go test -race ./...` 在 Windows 当前仅有 `D:\Work\MinGW` 的 `mingw32` 32 位工具链，执行时
  报 `cc1.exe: 64-bit mode not compiled in`；仍以 Ubuntu CI 为权威 race 证据。
- `origin/main`、`origin/develop`、`origin/yueyou_test` 均已包含 TTS 回归提交 `3c5351a`；远端 HEAD 仍为
  `yueyou_test`，GitHub 默认分支切换和分支保护尚未取得管理面证据。
- `v1.1.1` Tag 保持既有发布提交，未为后续治理修复重写 Tag；production Secrets、release-apk
  成功运行和 artifact SHA-256 仍未完成 GitHub 外部验收，OSS Bucket 私有策略与生命周期规则
  已完成生产配置并回读验证。
- 当前提交本地正式签名 APK 已构建并通过 APK v2 签名校验，大小 `25,856,776` 字节，SHA-256
  为 `603857D462DC3C24C163B0A077010D4FB1DC8288DDE298CD05AD919F7DA423AB`；该证据不替代
  GitHub production `release-apk` artifact。
- 已交叉编译并部署当前 Go 服务端至 `47.94.102.250:/www/wwwroot/yueyou/yueyou-server`，
  远端 `yueyou.service` active；公网 `hclstudio.cn` 已签发独立证书并反代至 `127.0.0.1:8081`。
- 公网验证：`/health`、`/ready`、`/privacy` 和 `/api/v1/book/catalog?bookId=xiyouji` 均返回 200。
- OSS 回读验证：Bucket ACL 为 `private`；`cache/tts/v2/` 生命周期规则为 Enabled、Expiration
  Days=1。远端旧二进制保留为 `yueyou-server.bak-79f0fe5`，可回滚。
- 生产 TTS 合成回归：合成测试音频下载成功（`audio/mpeg`、7200 字节），签名 URL 带查询参数；
  去除签名查询参数后 OSS 返回 `403`。远端日志仅记录 request ID、音色、字符数、字节数和耗时，
  未记录测试原文。

### 2026-07-12 提交后复核

- 当前 `origin/main`、`origin/develop`、`origin/yueyou_test` 均已指向
  当前治理提交，远端默认分支仍由 GitHub 仓库设置控制，当前
  权威结果仍为 `yueyou_test`。
- `SettingsScreen` Ubuntu 回归修复已提交：烟测改用显式初始化的 `SettingsProvider`，本地四条
  设置页测试、`flutter analyze`、`dart analyze test` 均通过；远端 CI 结果尚待公开运行完成确认。
- 本轮完成性审计确认仍不可标记为全局完成的证据项：GitHub 默认分支与分支保护、production
  signing Secrets 与 `release-apk` artifact、首次启动拒绝路径的真实零网络计数、G1 五人用户验证、
  两周 CI 稳定期，以及 `go test -race ./...` 的 Ubuntu 结果。

## 7. M2：架构边界收敛

### PR-8 App Composition 层

**目标**：把跨 feature 装配从 service、provider 和 shared widget 中收敛到应用组合根。

**实施项**：

- [x] 新增应用级 composition/bootstrap 目录，仅负责 Provider 装配和生命周期连接。
- [x] `shared/widgets` 禁止直接依赖 settings、game、audio feature。
- [x] 共享组件通过参数、接口或上层 wrapper 获取状态。
- [x] `ttsEngineProvider` 从 service 文件移动到 audio providers。
- [x] SettingsProvider 不再直接调用 AmbientService，由 composition 层 `ref.listen` 联动。
- [x] 增加 import boundary 自动检查。

**验收**：

- `core/`、`shared/` 不依赖具体 feature。
- service 不包含 Widget、BuildContext 或 Provider 声明。
- 生命周期释放链和 Provider override 测试全部通过。

**2026-07-12 执行记录**：

- 新增 `lib/app/composition/app_bootstrapper.dart`，集中应用主 MaterialApp、AmbientService
  设置联动、App 生命周期、当前书籍恢复和全局 TTS 用户活动监听；`main.dart` 保留启动闸门
  与 ProviderScope 外壳。
- 新增 `lib/features/audio/providers/tts_engine_provider.dart`，service 文件保留兼容导出；
  SettingsProvider 移除 AmbientService 直接调用，由组合根通过 `ref.listenManual` 联动。
- 新增 `lib/core/theme/cyber_animation_scope.dart`，共享弹窗和 Toast 通过 core 上下文获取
  动画等级；Toast 改为从调用上下文获取 Overlay；TTS 错误监听器归入 app composition。
- 新增 `SharedWidgetBoundaryRule` 与回归测试，自动阻断 `shared/widgets` 对具体 feature 或
  `main.dart` 的 import/export；当前边界检查通过。
- 验证：`flutter analyze`、`dart analyze test`、`dart scripts\ai_code_checker.dart`、
  `flutter test --concurrency=1`（689 passed、4 skipped）、
  `flutter test --coverage --concurrency=1`（690 passed、4 skipped）、
  `python scripts\check_coverage_gate.py --overall 80 --core 90`（总体 80.61%、核心均 >= 90%）、
  `go test ./...`、`go vet ./...`、`go build ./...` 全部通过。

### PR-9 2048 纯领域内核

**实施项**：

- [x] 抽出纯 Dart `GameEngine`，只负责棋盘、移动、合并、得分和游戏结束判断。
- [x] 随机数、持久化、音效、生命周期和 TTS 用户活动通知留在编排层。
- [x] Domain 层使用性质测试覆盖守恒、单次合并、顺序和边界不变量。
- [x] 使用 Transform/Opacity 重做滑动动画，不再由 AnimatedPositioned 触发布局。

**验收**：

- 纯 Dart 游戏测试无需 Flutter Binding。
- 棋盘算法与 UI 动画可以独立替换。
- Profile 模式滑动期间无持续布局抖动，目标设备 P95 帧耗时低于 16.7ms。

**2026-07-12 执行记录**：

- 新增纯 Dart `lib/features/game_2048/domain/game_engine.dart`，以不可变状态副本计算移动、
  合并、全盘求和、结束判断、添加和移除方块；领域文件不引入 Flutter。
- `GameProvider` 收敛为 Flutter 编排层，保留随机出生块、SharedPreferences 存档、音效、
  App 生命周期、ChangeNotifier 和 TTS 用户活动回调；文件由 547 行降至 322 行。
- 新增 `test/features/game_2048/game_engine_test.dart`，覆盖输入状态不变、单次合并、
  连续合并、无合并移动、满盘边界、添加/移除与计分等纯逻辑路径。
- `square_board.dart` 将方块从 `AnimatedPositioned` 迁移为固定尺寸
  `TweenAnimationBuilder<Offset> + Transform.translate`，并保留棋盘 `RepaintBoundary`。
- 验证：`flutter analyze`；`dart test test\features\game_2048\game_engine_test.dart`
  6/6 通过；全量 Flutter 测试 695 passed、4 skipped；覆盖率总体 80.79%，核心文件均 >= 90%；
  Go test/vet/build 全部通过。

### PR-10 TTS 与 Reader 职责评估

**原则**：不以压行数为目标，只处理真实职责混杂。

**候选边界**：

- TTS 执行引擎：播放、停止、速率、Wakelock。
- TTS 获取与缓存：POST URL、GET 下载、签名 URL 过期重取。
- TTS 会话编排：预加载、降级、取消、旧会话守卫。
- Reader：章节、游标、阅读进度，不直接感知播放器实现。

**拆分前必须回答**：

- [x] 新单元能否独立 mock 测试？
- [x] 原文件与新文件职责是否更清晰？
- [x] 是否避免循环依赖、回调爆炸和生命周期断链？

**2026-07-12 评估结论**：

- `TtsEngineService` 当前 578 行，播放、下载/缓存、诊断和降级已经分别委托给可注入的
  `TtsPlaybackController`、`TtsAudioDownloader`、`TtsDiagnosticsService` 等单元，相关测试
  可独立 mock；继续拆分会增加状态回写 callback 和 Riverpod 生命周期耦合，本轮不再拆。
- `ReaderProvider` 当前 667 行，职责集中在章节、游标、阅读进度与 TTS 会话编排，未发现可在
  不增加跨层 callback 的情况下独立抽出的真实边界；保留现状并禁止追加无关职责。
- PR-10 以评估完成收口，无新增代码拆分。

## 8. M3：听读产品主线验证

### 8.1 产品定位

主定位固定为“沉浸式本地小说听读伴侣”。2048 是听书时的可选陪伴模式，不再与阅读并列扩张为第二套产品。

### 8.2 优先能力

- [ ] 首次导入到开始朗读不超过 3 步。
- [ ] 睡眠定时、章节跳转、书签、历史位置和离线缓存稳定可恢复。
- [ ] TTS 弱网降级不重复朗读、不跳句、不丢进度。
- [ ] 游戏模式可完全隐藏，纯阅读用户不受干扰。
- [ ] 辅助功能覆盖字体缩放、屏幕阅读器标签、对比度和触控目标。
- [x] 选择 3 档清晰动画质量，移除无法用真实帧数据证明价值的同步 CPU 跑分。

### 8.3 验证方式

- 招募至少 5 名目标用户完成“导入一本书并听 10 分钟”的可用性测试。
- 全程不采集阅读正文；产品数据优先使用现场观察和用户主动反馈。
- 如引入匿名遥测，必须单独征得同意，只记录非内容事件，并更新隐私政策。

### 8.4 产品决策门 G1

以下条件全部满足才进入 AI 阶段：

- [ ] 5 名用户中至少 4 名无需指导完成导入和朗读。
- [ ] 10 分钟听读中无重复句、跳句和不可恢复中断。
- [ ] 至少 3 名用户明确愿意再次使用。
- [ ] P0/P1 发布问题保持关闭，连续两周 CI 稳定通过。

未通过时继续修复核心体验，不启动 AI 功能。

## 9. M4：AI 试点

### 9.1 唯一 MVP

首个 AI 能力只做“带章节引用的西游记上下文问答”，暂不同时做角色扮演、续写、摘要和 2048 策略。

**范围**：

- 用户针对当前章节或全书提问。
- 回答必须附章节号和可跳转原文片段。
- 检索无可靠证据时明确回答“未找到依据”。
- 会话历史设置长度与 Token 上限，不永久保存用户问题。
- AI 服务作为独立 Python 服务，通过内部 API 与 Go 网关协作。

### 9.2 AI 决策门 G2

- [ ] 预设事实题准确率不低于 85%。
- [ ] 引用章节命中率不低于 90%。
- [ ] P95 响应时间低于 5 秒。
- [ ] 单次问答成本和月度预算有硬上限。
- [ ] 5 名测试用户中至少 3 名认为问答提升了阅读体验。

未达标时停止扩展，不继续增加角色聊天和内容生成功能。

## 10. PR 切分与依赖顺序

```text
PR-0 计划书与审计基线
  -> PR-1 签名治理
  -> PR-2 隐私启动闸门
  -> PR-3 TTS 数据安全与防滥用
  -> PR-4 Android 备份边界
  -> PR-5 CI 与覆盖率
  -> PR-6 Go 服务端测试
  -> PR-7 Release 与分支治理
  -> PR-8 Composition 层
  -> PR-9 2048 纯领域内核
  -> PR-10 TTS/Reader 职责收敛
  -> G1 产品决策门
  -> M4 AI 问答 MVP
```

每个 PR 只解决一个主要问题，禁止把安全修复、架构重构和新功能混在同一 PR。

## 11. 角色与责任

| 角色 | 责任 |
| --- | --- |
| 项目负责人 | 签名轮换、应用市场、分支策略、隐私政策最终确认 |
| Flutter 负责人 | 启动闸门、Provider 生命周期、客户端测试、APK 构建 |
| 服务端负责人 | TTS 安全、限流、OSS 生命周期、Go 测试、部署与回滚 |
| 质量负责人 | CI、覆盖率口径、门禁、Release 验收、证据归档 |
| 产品负责人 | 听读主线、用户测试、G1/G2 决策，不以功能数量作为成功指标 |

当前为单人项目时，同一人可承担多个角色，但验收清单不可省略。

## 12. 每个 PR 的固定执行模板

### 开始前

- [ ] 更新当日唯一 `DevelopmentPlan/YYYYMMDD_*.md`。
- [ ] 检查 Git 状态，隔离用户已有改动。
- [ ] 确认受影响 Dart 文件行数和职责边界。
- [ ] 先写失败测试或明确不可自动化原因。

### 开发中

- [ ] 不改变无关行为。
- [ ] 异步流程有 disposed/session/request id 守卫。
- [ ] 异常通过 CyberLogger 或服务端结构化日志记录，禁止用户内容。
- [ ] 新配置通过环境变量或外部 Secret 注入。

### 完成后

- [ ] 更新任务单、`development_log.md`，必要时更新 README。
- [ ] 运行最小相关测试后再运行全量门禁。
- [ ] 记录命令、结果、覆盖率和已知风险。
- [ ] 中文 Conventional Commit，提交后推送当前分支。

## 13. 固定验证命令

```powershell
flutter analyze
dart scripts/ai_code_checker.dart
flutter test --coverage --concurrency=1
Push-Location server
go test -race ./...
go vet ./...
go build ./...
Pop-Location
flutter build apk --release --target-platform android-arm64 --split-per-abi
git status --short
```

涉及文档时额外执行 Markdown lint；涉及 Android 配置时增加 secret scan；涉及性能时必须使用 Profile 真机数据，不以 Debug 模式或单次主线程跑分作为结论。

## 14. 暂停与回滚条件

- 签名升级链不明确时，暂停更换 JKS，先确认应用市场规则。
- 隐私闸门导致启动死循环或无法恢复时，回滚该 PR，不允许临时恢复同意前网络初始化。
- TTS 私有 URL 导致播放失败时，回滚下载实现，不回滚原文脱敏和限流。
- 架构拆分引入生命周期竞态、循环依赖或测试行为变化时，整 PR 回滚并重新划分职责。
- AI 准确率、引用率、成本或用户价值未达到 G2 时停止扩展，保留稳定听读主线。

## 15. 本计划维护规则

- 本文件记录总体路线、里程碑状态和决策门结果。
- 每日实施继续遵守“一天一个 DevelopmentPlan 文件”，当天多个主题合并记录。
- 完成任务后将复选框更新为已完成，并附提交号、测试结果和证据路径。
- 计划调整必须说明原因、影响和新的验收标准，不允许静默删除未完成项。
- 下一次正式复盘时间：M1 完成后，或最迟 2026-07-18。
