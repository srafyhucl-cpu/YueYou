# 全局性能优化 P0/P1/P2 修复

## 背景

经过多轮全局性能深度审计（presentation/providers/services/domain/core/CustomPainter/AnimationController/Shader），
从全局用户感知视角重新分级，最终确认 4 项 P0 关键性能问题，本次一次性集中修复。

## 任务清单

### P0-A + P0-B：灵动岛（CyberPlayerConsole + NeonProgressPainter）

- [x] `dashboard_screen.dart`：灵动岛加 `RepaintBoundary` 隔离呼吸动画脏区
- [x] `cyber_player_console.dart`：呼吸动画按 TTS 状态（playing/buffering）启停，idle 时停止 60fps 驱动
- [x] `neon_progress_painter.dart`：4 个 Paint 实例静态复用 + LinearGradient shader 按 (size, color) 缓存

### P0-C：_MascotFacePainter RadialGradient shader 缓存

- [x] `board_mascot.dart`：`_drawCore` 中 RadialGradient shader 按 hasError 缓存，避免每帧 GPU shader 创建

### P0-D：CurvedAnimation 泄漏修复

- [x] `square_board.dart`：`_triggerTilt` 复用单个 `CurvedAnimation` + 目标值字段，不再每次滑动新建
- [x] `board_mascot.dart`：5 处 CurvedAnimation 持久复用（eye/body/expression/wobble/pulse），
  `_moveEyes`/`_triggerWobble`/`_setExpression` 仅更换 Tween 包装（轻量 `_AnimatedEvaluation`），dispose 统一清理

### P1-1：_MascotFacePainter Paint 对象复用

- [x] `board_mascot.dart`：5 个绘制方法从每帧 ~49 个 inline `Paint()` 改为 2 个 static 复用实例（`_fp` / `_sp`）

### P1-2：VoiceWaveform 动画启停

- [x] `voice_waveform.dart`：`didUpdateWidget` 按 `isActive` 启停 controller，空闲时不 tick

### P1-3：_RainPainter Paint 复用

- [x] `rain_effect.dart`：复用单个 static `_dropPaint`，消除每帧 20 个 Paint 分配

### P2-1：_ParticlePainter Paint 复用

- [x] `merge_particle.dart`：复用 2 个 static Paint（`_corePaint` / `_glowPaint`），消除每帧 16 个 Paint 分配

### P2-2：CurvedAnimation 显式 dispose

- [x] `floating_score.dart`：2 个 CurvedAnimation 存引用并显式 dispose
- [x] `board_reset_animation.dart`：1 个 CurvedAnimation 存引用并显式 dispose
- [x] `cyber_toast.dart`：2 个 CurvedAnimation 存引用并显式 dispose

## 验收标准

- [x] `flutter analyze` 零错误零警告
- [x] `dart scripts/ai_code_checker.dart` 门禁通过
- [x] `flutter test` 669 通过、零失败
- [ ] dashboard 静置 1 分钟 GPU 平均 < 1ms/帧
- [ ] 游戏连续滑动 30 次 GPU 峰值 < 12ms/帧
- [ ] 30 分钟游戏后 AnimationController listener 数量稳定
- [ ] 所有现有视觉效果像素级一致

## 涉及文件

- `lib/features/dashboard/presentation/dashboard_screen.dart`
- `lib/features/audio/presentation/widgets/cyber_player_console.dart`
- `lib/features/audio/presentation/widgets/neon_progress_painter.dart`
- `lib/features/audio/presentation/widgets/voice_waveform.dart`
- `lib/features/game_2048/presentation/widgets/square_board.dart`
- `lib/features/game_2048/presentation/widgets/board_mascot.dart`
- `lib/features/game_2048/presentation/widgets/rain_effect.dart`
- `lib/features/game_2048/presentation/widgets/merge_particle.dart`
- `lib/features/game_2048/presentation/widgets/floating_score.dart`
- `lib/features/game_2048/presentation/widgets/board_reset_animation.dart`
- `lib/shared/widgets/cyber_toast.dart`
