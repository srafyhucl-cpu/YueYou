# 20260714 IP-0 资产治理与性能门执行

## 任务范围

按照 `20260713_性能架构原型与改进报告.md` 的当前后续切片，执行 Xiaoyo IP-0
资产治理前置工作。范围仅包含清单、证据要求和自动化门禁，不生成原创美术、不修改
社区 Rive、不接入支付/权益/GLB/Unity，也不修改用户已有版权材料与 `server_py/`。

## 已完成

- [x] 建立机器可读 IP-0 manifest，明确候选图、社区 Rive、商业使用批准和六类证据状态。
- [x] 建立资产治理说明，固定四视图、表情、材质、动作、近似检查和授权链的验收口径。
- [x] 建立 `check_xiaoyo_ip0.dart`，默认结构校验，`--require-accepted` 严格门禁。
- [x] 增加四条纯 Dart 测试路径，覆盖未就绪、绕过批准、社区资源误标和完整 accepted。
- [x] 同步总详设第 42 节；未修改现有版权材料。

## PROD-09-A 作者听校本地领域基础

- [x] 新增纯 Dart `AuthorReviewMark` 和 `AuthorReviewSession`，覆盖标记、定位、复核和
  本地导出恢复。
- [x] 新增 `AuthorReviewRepository` 与本地文件仓储，支持主文件、`.bak`、`.tmp` 原子
  写入和损坏回退；测试通过内存文档存储替身完成。
- [x] 导出只包含章节定位、问题类型、备注和状态，不包含正文摘录、路径、账号或网络字段。
- [x] 新增 10 项领域/仓储测试；未接入 UI、Provider、服务端、支付或普通读者主流程。
- [ ] 作者 UI、真实作者样本、人工交付、49 元真实购买和退款证据仍未提供。

## PROD-07-B 匿名商业证据计算器

- [x] 新增匿名聚合计数计算器，固化第 21.2 节指标、前置条件、退款净额和总停止线。
- [x] 缺失数据只输出 `insufficientEvidence`，前置条件未通过只输出 `notStarted`；拒绝
  正文、书名、路径、账号、设备和用户级标识字段。
- [x] 新增 9 项边界测试；没有创建 `commerce`、订单、权益、支付 SDK 或远程请求。
- [ ] 真实支付、退款、D0/D7、付款理由和云音色外部证据仍未提供。

## PROD-05-C Xiaoyo 本地成长数据导出与恢复

- [x] 价值系统开启后新增本地 JSON 导出与恢复入口，复用 checksum/schema 校验，限制文件为 1 MiB。
- [x] 恢复前二次确认；取消、损坏或仓储失败均保留当前 Profile，并沿用本地 `.bak` 备份。
- [x] 新增服务、Widget、Provider 替换和校验失败测试；README 已同步用户可见能力。
- [ ] 真实 Android/iOS 文件选择器和换机闭环尚未验收，当前默认开关仍关闭。

## PROD-00-P 手机 HTML 原型结构合同检查

- [x] 新增零依赖 Python `HTMLParser` 检查器和 2 项单测，验证 390 x 844、导航、场景、
  陪伴/书境分段、关键出口、脚本绑定和本地图片。
- [x] 当前检查 `ok=true`，结构统计为 6 场景、3 导航、3 陪伴分段、2 书境分段、5 图片。
- [ ] 浏览器自动点击、像素检查、Android/iOS 真机和支付行为仍未验证。

## PROD-00-P-1 原型脚本交互与网络边界门

- [x] 将 21 个直接按钮事件和 7 个委托事件纳入静态合同检查，防止节点存在但脚本未绑定。
- [x] 将 `fetch`、XHR、WebSocket、Beacon 和 Payment Request API 纳入阻断检查；原型必须
  明确支付仅为模拟反馈且不会发起支付。
- [x] 增加缺失事件绑定和网络 API 注入两条回归测试；Python 单测 `4 passed / 0 failed`，
  当前原型检查 `ok=true`、`errors=[]`、`warnings=[]`。
- [ ] 浏览器自动点击、焦点/滚动、像素检查、Android/iOS 真机和支付行为仍未验证。

## PROD-00-P-2 引用截图尺寸合同

- [x] 校验用户引用的桌面截图 PNG 尺寸为 `1440x1000`、手机截图 PNG 尺寸为 `390x844`，并
  检查 PNG 头和尺寸字段可读。
- [x] 增加缺失截图回归测试；Python 单测 `5 passed / 0 failed`，当前原型检查保持
  `ok=true`、`errors=[]`、`warnings=[]`。
- [ ] 尺寸合同不替代浏览器像素内容、点击路径、焦点/滚动、Android/iOS 真机和支付验收。

## 本次收口验证

- [x] `flutter analyze`：通过，无问题。
- [x] `flutter test --concurrency=1 --reporter compact`：`753 passed / 4 skipped / 0 failed`。
- [x] `dart scripts/ai_code_checker.dart`：0 阻断、22 个既有警告，门禁通过。
- [x] `cd server; go vet ./...` 与 `go build ./...`：通过。
- [x] Markdown linter、Python 原型检查和 `git diff --check`：通过。

## 当前停止线

- IP-0 未 accepted 前，不生成商业 `xiaoyo_v2_base.riv`，不进入 GLB/Unity 和批量皮肤。
- 真实支付、退款、D0/D7 回访和付款理由证据未齐前，不创建正式 commerce。
- 没有物理 Android 设备和负责人 G0 签署前，不宣称性能收益。

## 验证命令

```powershell
flutter test test/scripts/check_xiaoyo_ip0_test.dart --concurrency=1
dart analyze scripts/check_xiaoyo_ip0.dart
dart run scripts/check_xiaoyo_ip0.dart
git diff --check
```

## 待外部证据

资产负责人需要提交六类 IP-0 证据、原创源文件、授权凭证、资产哈希和签署记录；这些
材料不由工程门禁代填，也不以候选图或社区资源替代。
