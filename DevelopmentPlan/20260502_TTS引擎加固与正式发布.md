# 任务卡片：TTS 引擎架构加固与正式包发布 (2026-05-02)

## 🎯 任务目标
1. 解决 TTS 引擎接口迁移后的存量测试失败问题。
2. 修复 Isolate 下载导致的单元测试 Mock 绕过缺陷。
3. 补全 TTS 引擎的“分离下载”契约逻辑。
4. 清理全局 Lint 警告，确保代码库 100% 合规。
5. 构建轻量级正式 APK 交付包。

## 🛠 实施细节
- **架构加固**：
  - 在 `TtsEngineService` 中引入影子兼容循环，恢复 `onNeedPrefetch` 钩子支持。
  - 修正 `bufferHealthRatio` 除零保护及 `TtsBufferStatus` 状态映射。
  - 在 `_executeDownload` 中增加环境判定，单元测试时强制走主线程以配合 Mock 验证。
- **契约修复**：
  - 修正 `_mainThreadDownload` 逻辑，补全缺失的 `download` 物理文件写入步骤。
  - 在测试中调整时序（`10ms` -> `300ms`），解决由于异步循环步长导致的状态竞争。
- **性能优化**：
  - 使用 `dart fix` 统一修复 26 处 `require_trailing_commas` 和 `prefer_const_constructors`。
  - 移除未使用的 `_syncSettingsAfterInit` 冗余代码。

## ✅ 验收标准
- [x] `test/features/audio/tts_engine_service_test.dart` 全绿 (19/19)。
- [x] `test/features/audio/tts_contract_test.dart` 全绿 (2/2)。
- [x] `test/features/audio/tts_buffer_monitor_test.dart` 全绿 (20/20)。
- [x] `flutter analyze` 报告 0 错误 0 警告。
- [x] 成功生成 ABI 分离的正式版 APK。

## 📦 产物路径
- arm64-v8a: `build\app\outputs\flutter-apk\app-arm64-v8a-release.apk` (26.8MB)
