# 任务单：TTS 引擎加固与正式发布 (20260502)

## 🎯 任务目标

- [x] **错误隔离**：彻底解决切换书籍/章节时的 TTS 降级状态残留。
- [x] **物理进度同步**：实现提词器与底层音频播放头的绝对同步（KTV 模式）。
- [x] **省电管理**：实现全局静默自动停播逻辑。

## 🏗 核心修复逻辑

- **[修复] 会话原子化**：在 `refreshSession` 中强制递增 `_session` 并清理所有缓冲区，防止旧回调干扰。
- **[修复] 物理驱动同步**：通过监听 `onPositionChanged` 实现提词器渲染，精度达到毫秒级。
- **[优化] 视觉反馈**：重构 `TeleprompterView`，增加平滑滚动与高亮渐变。

## 📅 进度追踪

- [x] TTS 状态机重构（Sealed Classes + Riverpod）
- [x] 影子状态同步逻辑（Shadow Sync）
- [x] 物理进度监听器集成
- [x] 验证：通过 `tts_contract_test.dart` 100% 通过

## ⚠️ 注意事项

- **GPU 负载**：提词器高频刷新时，务必使用 `RepaintBoundary` 进行隔离。
- **内存安全**：下载的 MP3 临时文件必须遵循“阅后即焚”原则，在 `_deleteFile` 中及时回收。
