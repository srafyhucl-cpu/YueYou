# 2026-05-04 静态分析止血与契约恢复

## 任务目标

- **定位原因**：分析 `flutter analyze` 大面积报错的根因，区分真实错误与修复过程中引入的契约漂移。
- **执行止血**：停止逐个补错策略，优先恢复被重建破坏的核心源码契约。
- **验证结果**：确保工作区回到可诊断、可交付的零警告状态。

## 根因结论

- **核心问题**：多文件被删除重建后，`TtsEngineService`、`TtsAudioNotifier`、`TileModel` 等核心 API 与测试契约发生漂移。
- **扩散原因**：模型构造函数、服务构造参数、Provider 依赖和主题常量被局部补丁改动后，引发级联静态分析错误。
- **止血策略**：不继续按单条报错补 API，而是恢复 Git 基线，回到原始业务契约。

## 执行记录

- **恢复高扩散文件**：先恢复 TTS、2048、配置与音频服务相关文件，错误数由约 1129 收敛至 437。
- **全量止血回滚**：执行 `git checkout -- .` 恢复所有已跟踪文件到 Git 基线。
- **静态分析验证**：执行 `flutter analyze`，结果为 `No issues found!`。
- **README 评估**：本次无功能变更、无架构变更，README 无需更新。
- **规范化第 1 批**：按批次治理计划处理 `SfxService` 与 `AmbientService` 的非法控制台输出，替换为 `CyberLogger.captureMessage()` 与 `CyberLogger.captureWarning()`，未改变公开 API 与音频行为。
- **批次验证**：目标文件已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/audio/tts_audio_notifier_test.dart --concurrency=1` 通过。
- **风险记录**：`flutter test test/features/audio --concurrency=1` 在 `tts_engine_service_test.dart` 存在非本批引入失败，后续进入 TTS 专项批次单独定位。
- **规范化第 2 批**：处理 `DefaultBookService` 的默认书籍目录、章节下载、缓存写入与影子预读日志，统一替换为 `CyberLogger.captureWarning()`，保留缓存命中、分离下载和降级内置常量行为。
- **批次验证**：`DefaultBookService` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/library --concurrency=1` 通过。
- **后续候选**：`FileImportService` 在 library 测试中仍有既有控制台异常输出，后续作为独立批次处理。
- **规范化第 3 批**：处理 `FileImportService` 的 TXT 导入、Isolate 解析、主动取消和流式解析日志，替换为 `CyberLogger.captureWarning()` / `CyberLogger.captureMessage()`，并将文件不存在场景作为可预期输入直接返回 `null`。
- **批次验证**：`FileImportService` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/library/file_import_service_test.dart --concurrency=1` 通过且无异常栈噪声。
- **规范化第 4 批**：处理 `BookshelfProvider` 的删除书籍 best-effort 清理和默认书籍注入失败日志，统一替换为 `CyberLogger.captureWarning()`，保持书架即时移除、级联重置和失败降级行为不变。
- **批次验证**：`BookshelfProvider` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/library/bookshelf_provider_test.dart --concurrency=1` 通过。
- **后续候选**：书架测试中仍暴露 `ReaderProvider` 与 TTS 降级引擎既有控制台输出，后续单独治理。
- **规范化第 5 批**：处理 `UpdateService` 的版本检测日志，未配置 `UPDATE_API_URL` 时保持静默返回，HTTP 异常和检测异常改用 `CyberLogger.captureWarning()`，版本状态改用 `CyberLogger.captureMessage()`。
- **批次验证**：`UpdateService` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/update/update_service_test.dart --concurrency=1` 通过。
- **规范化第 6 批**：处理 `GameProvider._loadSavedState` 的游戏快照恢复异常日志，替换为 `CyberLogger.captureWarning()`，携带 stack trace 和 tag=game，行为保持快照解析失败后降级新局不变。
- **批次验证**：`GameProvider` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/game_2048/game_provider_test.dart --concurrency=1` 通过（44 个测试）。
- **规范化第 7 批**：处理 `TtsErrorListener` 的 4 处日志：删除 `build()` 中调试构建追踪日志，检测新错误改为 `CyberLogger.captureMessage()`，两处 CyberToast 展示失败 catch 块改为 `CyberLogger.captureWarning()`，携带 tag=tts。
- **批次验证**：`TtsErrorListener` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/audio/tts_audio_notifier_test.dart --concurrency=1` 通过。
- **规范化第 8 批**：处理 `BoardMascotRive._loadRiveFile` 的 8 处日志：删除 Rive 加载成功、Artboard 名称、状态机连接成功、可用输入列表等纯调试日志；将输入未找到、状态机未找到、加载异常改为 `CyberLogger.captureWarning()`，携带 tag=game。
- **批次验证**：`BoardMascotRive` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过。
- **规范化第 9 批**：处理 3 个低风险单点日志并拆顺带修复存量 lint：`CyberPerformanceDetector` 将性能基准日志改为 `CyberLogger.captureMessage()`；`DashboardScreen` 删除 XIAOYO 点击调试追踪日志并修复上搅弹框的 4 处 `require_trailing_commas` lint；`LibraryScreen` 删除 `_loadBook` 调试追踪日志。
- **批次验证**：3 个文件已无 `debugPrint()` / `print()`；`flutter analyze` 通过。
- **规范化第 10 批**：处理 `StorageService`（8 处）和 `TtsCacheManager`（7 处）的日志：所有 catch 块替换为 `CyberLogger.captureWarning()`，定时清理启动和清理完成摘要改为 `CyberLogger.captureMessage()`，缓存超限不是异常改用 `captureWarning(StateError(...))`，两个文件均保留 `flutter/foundation.dart`（`@visibleForTesting` / `kIsWeb` 依赖）。
- **批次验证**：`StorageService` 和 `TtsCacheManager` 已无 `debugPrint()` / `print()`（注释示例除外）；`flutter analyze` 通过。
- **规范化第 11 批**：处理 `TtsAudioNotifier` 的 7 处日志：空闲停播、暂停中断保留进度、网络恢复退出降级改为 `captureMessage()`；预加载异常、播放异常、删除临时文件失败改为 `captureWarning()`，均补 stack；销毁临时文件成功清理纯调试日志直接删除；移除已无用的 `flutter/foundation.dart` import。
- **批次验证**：`TtsAudioNotifier` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/audio/tts_audio_notifier_test.dart --concurrency=1` 通过（测试存在 timing-sensitive 偶发性，与日志替换无关）。
- **规范化第 12 批**：处理 `TtsEngineService` 共 36 处 `debugPrint`：删除 `_RealHttpClient` 中下载进度、重定向、连接成功、数据块、响应完成、POST 请求/响应等纯调试追踪日志；删除 `_executeDownload` Isolate 成功日志、`playFile` 启动日志、超时/异常重复日志（下方已有 `CyberLogger`）、文件不存在静默跳过日志；将本地 TTS 超时/错误、降级引擎初始化失败、引擎初始化失败、降级朗读失败、设置倡速/音量失败、清理残留文件失败、重试各层异常、Isolate 失败降级、兑容循环异常改为 `captureWarning()`；引擎已初始化、清理残留文件成功、兑容循环短句过滤改为 `captureMessage()`；顺带修复 3 处 `require_trailing_commas` lint。
- **批次验证**：`TtsEngineService` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/audio/ --concurrency=1` 90 个测试全部通过。
- **规范化第 13 批**：处理 `ReaderProvider` 共 22 处 `debugPrint`：删除 `nextTtsSentence` 末尾进度提交追踪、已到达末尾静默返回、`resetFetchIndex` 重置追踪、`loadBook`/`loadChapter` 调用追踪、`fetchChapter` 返回、`loadBook` 完成、`jumpTo` 成功追踪等纯调试日志；将 4 处 `_saveProgress` 失败 catchError、`loadPreparedBook` 异常、`loadBook` 异常、`loadChapter` 失败改为 `captureWarning()`；将 `jumpTo` 越界改为 `captureWarning(StateError(...))`；将级联重置完成、`restoreDefaultBook` 恢复、章末已到最后一章、章末自动推进改为 `captureMessage()`；引入 `cyber_logger.dart` import；顺带修复 1 处 `require_trailing_commas` lint。
- **批次验证**：`ReaderProvider` 已无 `debugPrint()` / `print()`；`flutter analyze` 通过；`flutter test test/features/reader/ --concurrency=1` 75 个测试全部通过。
- **阶段 2（URL 审计）**：全仓 12 处 `https://` 扫描结论：全部合规，均为文档注释示例、`String.fromEnvironment` 生产默认值、或合规的外部跳转链接，无需代码改动。
- **阶段 3 第 1 批（UI 动画常量化）**：向 `CyberDimensions` 追加 8 个动效时长常量（`animNormal` 300ms、`animFast` 250ms、`animXFast` 150ms、`animInstant` 120ms、`animMedium` 400ms、`animEliminate` 450ms、`animSlow` 600ms、`toastDuration` 2500ms）；替换 `cyber_toast.dart`（2 处）、`cyber_modal.dart`（1 处）、`settings_screen.dart`（2 处）、`chapter_list_screen.dart`（2 处）、`tile_widget.dart`（2 处）、`square_board.dart`（2 处）、`merge_particle.dart`（1 处）、`board_reset_animation.dart`（1 处）、`board_mascot.dart`（1 处）共 14 处硬编码 Duration；顺带修复 `square_board.dart` 和 `settings_screen.dart` 合计 10 处存量 `require_trailing_commas` lint；单次出现且语义独特的时长（480ms 跳跃、1200ms 漂浮、1500ms 彩蛋重置）保留不变。
- **批次验证**：`flutter analyze` 零警告；`flutter test test/features/game_2048/ test/features/reader/ --concurrency=1` 157 个测试全部通过。
- **阶段 3 第 2 批（网络超时常量化）**：向 `TtsConfig` 追加 6 个网络层超时常量：`ttsLocalSpeakTimeout`(60s)、`ttsDownloadTimeout`(15s)、`ttsPostConnectionTimeout`(10s)、`ttsPostResponseTimeout`(15s)、`bookApiTimeout`(4s)、`bookCdnDownloadTimeout`(15s)；替换 `tts_engine_service.dart` 5 处、`default_book_service.dart` 3 处共 8 处硬编码超时；顺带修复 `tts_engine_service.dart` 中字符串插值遗漏括号 bug（`$_config.maxRetries` → `${_config.maxRetries}`）。
- **批次验证**：`flutter analyze` 零警告；`test/features/audio/` 中存量 -1 偶发 timing 失败（git stash 验证与本批无关，早于本批已存在）；分模块单独运行全部通过。
- **测试偶发修复**：`sfx_service_test` `playerChannel` mock 生命周期提升到 `setUpAll/tearDownAll`，消除 `AudioPlayer.PositionUpdater` 跨测试 timer 泄漏；`tts_audio_notifier_test` 等待条件改为 `startedCalls >= 1` 并扩大超时至 5s；8 连跑验证全部稳定通过。
- **阶段 4（异常处理统一审计）**：全库 `catch` 扫描，识别 5 处缺失 stack 或缺 `CyberLogger` 的问题：`tts_engine_service.dart` `testConnection` 步骤 5 写入失败、`TimeoutException`、`SocketException`、兜底 catch 补 `stack: st` + `CyberLogger`；`playFile` `TimeoutException` 和通用 catch 补 `stack: st`；`tts_audio_notifier.dart` `downloadAudio` catch 补 `stack: st`；`settings_screen.dart` `_testTtsConnection` catch 补 `CyberLogger` + `stack: st` + import；确认 12 处 `catch (_) {}` 均为析构/尽力删除/降级 ping 等合规静默处理。
- **批次验证**：`flutter analyze` 零警告；`flutter test test/features/audio/ --concurrency=1` 90 个测试全部通过。
- **阶段 5（动态类型/强制解包/序列化边界审计）**：全库扫描 128 处 `!` 强制解包、60 处 `Map<String, dynamic>`、所有 `as` 转型。结论：全部合规——`!` 均有 Completer 赋值/null guard/内部构造 Map 键必存在等来源约束；`as` 均在序列化边界或 null check 后；`Map<String, dynamic>` 均在持久化/网络响应解析层；无 `TODO/FIXME/HACK/ignore` 遗留标记。无需任何修改。
- **阶段 6（资源/平台/构建配置审计）**：`pubspec.yaml` 资源声明 `assets/rive/` 与实际文件（`xiaoyo.riv`、`icon.png`）完全匹配；`board_mascot_rive.dart` Rive 加载有 try/catch 降级保护；所有 dependencies 均有实际使用；Android 构建命令规范已固化在 AGENT.md；无冗余声明、无路径不一致问题。无需任何修改。
- **批次验证**：`flutter analyze` 零警告，无代码改动故无需额外测试。
- **阶段 8（架构边界/分层违规审计）**：四层全面扫描结论：`core/` 无任何 `features/` 引入；`domain/` 无 `package:flutter/material` 引入（`update_info.dart` 仅引入 `foundation.dart` 用于 `@immutable`，合规豁免）；`providers/` 无 UI 布局代码（`game_provider.dart` 的 `WidgetsBindingObserver` 属于生命周期监听，合规）；`shared/widgets/tts_error_listener.dart` 引入 `features/audio` 属跨 feature 监听组件，具备合理业务语义；`library_screen` 直调 `StorageService.getCurrentChapterIndex()` 与 `ReaderProvider.restoreDefaultBook()` 内部行为平行，有意设计；`ref.onDispose` 居用：`readerProvider` 和 `ttsAudioProvider` 均正确注册。无新增分层违规，无需任何修改。
- **批次验证**：`flutter analyze` 零警告，全阶段规范化治理内容全部完成。

## 验收结果

- [x] `flutter analyze` 零错误零警告。
- [x] 工作区源码恢复到干净基线。
- [x] 明确大面积报错根因为契约漂移与编码/重建叠加。
- [x] 已停止继续扩大补丁面。
- [x] 规范化第 1 批保持最小改动与零静态分析告警。
- [x] 规范化第 2 批保持默认书籍服务契约不变。
- [x] 规范化第 3 批保持 TXT 导入解析契约不变。
- [x] 规范化第 4 批保持书架删除与默认书注入契约不变。
- [x] 规范化第 5 批保持版本检测静默降级契约不变。
- [x] 规范化第 6 批保持游戏快照解析失败降级契约不变。
- [x] 规范化第 7 批保持 TTS 错误监听行为不变（错误/降级通知仍正常弹出）。
- [x] 规范化第 8 批保持 Rive 吸照载失败降级行为不变（展示加载中转态，加载失败不崩溃）。
- [x] 规范化第 9 批保持性能基准检测、吸照点击、书架加载行为不变，并修复了存量 lint。
- [x] 规范化第 10 批保持持久化和缓存清理失败降级契约不变。
- [x] 规范化第 11 批保持 TTS 播放状态机商业契约不变（暂停中断、降级自动停播行为保持）。
- [x] 规范化第 12 批保持 TTS 播放、重试、Isolate 降级、兑容循环契约不变，90 个测试全通过。
- [x] 规范化第 13 批保持阅读器切套、跨章跳转、章末自动推进、默认书恢复契约不变，75 个测试全通过。
- [x] 阶段 2 URL 审计：全仓 12 处 https:// 均合规，无需改动。
- [x] 阶段 3 第 1 批 UI 动画常量化：抽取 8 个常量，替换 9 个文件 14 处硬编码 Duration，顺带修复 10 处 lint，157 个测试全通过。
- [x] 阶段 3 第 2 批 网络超时常量化：TtsConfig 追加 6 个超时常量，替换 2 个文件 8 处硬编码，顺带修复字符串插值 bug。
- [x] 测试偶发修复：sfx mock 生命周期提升，notifier 等待条件精确化，8 连跑稳定通过。
- [x] 阶段 4 异常处理审计：5 处补 stack/CyberLogger，12 处合规静默确认，flutter analyze 零警告，90 个音频测试全通过。
- [x] 阶段 5 动态类型/强制解包审计：128 处 `!`、60 处 `Map<String, dynamic>`、全部 `as` 转型均合规，无需修改。
- [x] 阶段 6 资源/构建审计：资源声明与实际文件一致，Rive 降级保护完整，依赖无冗余，无需修改。
- [x] 阶段 8 架构边界审计：四层分层均满足约束，ref.onDispose 居用正确，无新增违规，无需修改。
- [x] 权限合规审查：file_picker 走 SAF 无需运行时权限合规；隐私弹窗补合规标题"阅游 · 隐私政策"+ 开发者信息节（运营主体 / 联系邮箱通过 `--dart-define` 注入）+ 按钮措辞优化（同意 / 不同意并退出）；main.dart 修复 navContext null 静默跳过 bug，改为重试 5 次 + 兜底退出；452 个测试全通过。
- [x] 中国应用商店上架预检与软著材料：新增 release 签名模板、R8 压缩配置、应用描述资源、MIT LICENSE、商店文案模板；生成 `docs/copyright/源代码.pdf`（60 页）及软著操作说明书、申请清单、使用说明。
