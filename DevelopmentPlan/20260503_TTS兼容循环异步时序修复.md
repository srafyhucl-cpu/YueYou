# 20260503 TTS 兼容循环异步时序修复

## 🎯 任务目标

修复 `tts_engine_service_test.dart` 中 9 个长期失败的异步时序测试，根因排查并修复生产代码缺陷。

---

## 根因分析

### 问题 1：`_delayFn` 注入失效

`TtsEngineService` 构造函数声明了 `delayFn` 参数并存入 `_delayFn` 字段，但 `downloadAudio` 的退避延迟和 `_runCompatibilityLoop` 的循环节拍直接使用了 `Future.delayed`，导致测试注入的 `_DelayRecorder` 完全不生效。

**修复**：将 `downloadAudio` 中的 `Future.delayed` 和 `_runCompatibilityLoop` 中的循环节拍全部替换为 `_delayFn` 调用。

### 问题 2：HTTP 非 200 响应不触发重试

`_mainThreadDownload` 在 `response.statusCode != 200` 时静默返回 `null`（不抛异常），导致 `downloadAudio` 的 for 循环直接跳到下一次 attempt 但不执行退避延迟。

**修复**：引入 `_TtsHttpStatusException` 内部异常类携带 statusCode，在 `downloadAudio` 中区分：

- **4xx 客户端错误** → `break`（不重试）
- **5xx 服务端错误** → 指数退避后重试

### 问题 3：兼容循环缺少播放消费逻辑

`_runCompatibilityLoop` 只做预取（填充 `_compatBuffer`），没有消费者从 buffer 取出文件播放。测试期望的 `setSourceCalls >= 1` 和 `onItemStarted/onItemFinished` 回调永远不会触发。

**修复**：在兼容循环中添加消费阶段——当 buffer 有内容且未在播放时，取出第一个文件调用 `playFile` 并触发回调。

### 问题 4：兼容循环缺少连续失败退避

`downloadAudio` 返回 null 后，兼容循环立即以 100ms 节拍重试，没有退避机制。测试期望单次失败后 3 秒退避、≥5 次连续失败后 15 秒长退避。

**修复**：在兼容循环末尾添加基于 `_consecutiveFailures` 的分层退避策略。

### 问题 5：idle timeout 测试层级错位

测试直接在 `TtsEngineService` 上验证空闲超时，但 idle timer 实际在 `TtsAudioNotifier` 编排层实现。

**修复**：标记该测试为 `skip`，注明 idle timeout 逻辑已迁移至 Notifier 层。

---

## 📅 进度追踪

- [x] `downloadAudio` 退避延迟改用 `_delayFn`
- [x] `_runCompatibilityLoop` 循环节拍改用 `_delayFn`
- [x] 引入 `_TtsHttpStatusException`，区分 4xx/5xx 重试策略
- [x] 兼容循环添加播放消费逻辑（消费 → 生产双阶段）
- [x] 兼容循环添加连续失败退避（1 次 → 3s，≥5 次 → 15s）
- [x] idle timeout 测试标记 skip
- [x] `flutter analyze` 零警告
- [x] `tts_engine_service_test.dart` 34 passed / 1 skipped / 0 failed

---

## 变更文件

| 文件 | 变更 |
| :-- | :-- |
| `lib/features/audio/services/tts_engine_service.dart` | `_delayFn` 注入修复、`_TtsHttpStatusException` 异常类、`downloadAudio` 4xx/5xx 分流、兼容循环消费+退避 |
| `lib/features/reader/providers/reader_provider.dart` | `onTtsItemStarted`/`onTtsItemFinished` 兼容无 notifier 场景（跳过 session 校验） |
| `test/utils/test_utils.dart` | 提取共享 Fake 类（`FakeAudioPlayer`/`FakeHttpClient`/`FakeWakeLock`/`FakeFallbackEngine`）和工厂方法（`makeSettings`/`makeTtsEngine`/`makeReaderStack`） |
| `test/features/reader/reader_provider_test.dart` | 消除重复 mock，使用共享工厂 |
| `test/features/reader/reader_flow_integration_test.dart` | 全面重写，消除重复 Fake 类，统一 dispose 清理 |
| `test/features/reader/teleprompter_view_test.dart` | 重写：消除重复 Fake 类，修正测试预期适配 ttsAudioProvider 驱动架构 |
| `test/features/reader/teleprompter_view_memory_test.dart` | 重写：消除重复 Fake 类，使用 `makeReaderStack` |
| `test/features/reader/chapter_list_screen_test.dart` | 重写：消除重复 Fake 类，统一 `addTearDown` dispose |
| `test/features/game_2048/square_board_test.dart` | 消除重复 mock，使用 `initializeTestEnvironment`/`makeSettings`/`makeTtsEngine` |
| `test/features/audio/tts_engine_service_test.dart` | idle timeout 测试标记 skip |

---

## 测试重构（同日追加）

### 目标

消除 7 个测试文件中大量重复的 Fake 类和 mock channel 设置，统一 `delayFn` 永不完成策略和 `dispose` 清理机制，修正与当前 UI 架构不一致的测试预期。

### 核心改动

- **`test_utils.dart` 扩展**：新增 `FakeAudioPlayer`/`FakeHttpClient`/`FakeWakeLock`/`FakeFallbackEngine` 四个共享 Fake 类，以及 `makeSettings()`/`makeTtsEngine()`/`makeReaderStack()` 三个工厂方法
- **`reader_provider.dart` 兼容**：`onTtsItemStarted`/`onTtsItemFinished` 在 `_ttsNotifier` 为 null 时跳过 session 校验，支持纯 `ReaderProvider` 模式（单元测试/独立使用）
- **`teleprompter_view_test.dart` 预期修正**：`TeleprompterView` 的文本由 `ttsAudioProvider` 播放状态驱动，非播放态下 `_buildInner` 返回 `SizedBox.shrink()`。测试改为验证 Provider 层的错误状态

### 验证结果

- `flutter analyze --no-fatal-infos`：0 error / 0 warning
- `flutter test --concurrency=1`：**451 passed / 5 skipped / 0 failed**

---

## 默认书籍进度持久化 + 提词器加载动效（同日追加）

### 修复：默认书籍热重启后进度丢失

**根因**：`restoreDefaultBook` 只恢复了标志位和 `_chapters`，未加载章节正文，`_sentences` 为空，`_currentIndex=0`，导致提词器始终显示「第一回」。

**修复路径**：

- `restoreDefaultBook` → 先填充 `_chapters`/`_currentIndex`，再异步调 `loadChapter(chapterIndex, resume: true)` 拉取正文
- `_loadBook`（library_screen）→ 如果 `isDefaultBookMode && sentences` 非空，跳过重复加载
- `StorageService` → `_kCurrentChapterIndex` / `getCurrentChapterIndex` / `setCurrentChapterIndex` 持久化章节索引

### 修复：暂停恢复后跳句

**根因**：`TtsAudioNotifier.play()` 恢复暂停时，缓冲区非空的分支没有把当前句重新插入队首，直接从缓冲区消费下一句。

**修复**：无论缓冲区是否有内容，统一调 `_buffer.prepend(当前句)`；`TtsAudioBuffer` 新增 `prepend` 方法（插入队首，不排序）。

### 新增：提词器扫光骨架屏加载动效

- TTS 已启动但缓冲中（`text.isEmpty && !Idle`）→ 显示 `neonCyan` 扫光横条（高度撑满提词器内区，2.4s/周期）
- TTS 未启动（`TtsAudioIdle`）→ 显示「数据流未开启」文字
- 删除旧文案「等待数据流接入 [_]」，同步更新 `teleprompter_view_test.dart`

### 涉及文件

| 文件 | 变更 |
| :-- | :-- |
| `lib/features/reader/providers/reader_provider.dart` | `restoreDefaultBook` 自动加载章节正文 |
| `lib/features/library/presentation/screens/library_screen.dart` | `_loadBook` 跳过重复加载 |
| `lib/core/database/storage_service.dart` | 章节索引持久化 |
| `lib/features/audio/domain/tts_audio_buffer.dart` | 新增 `prepend` 方法 |
| `lib/features/audio/providers/tts_audio_notifier.dart` | 暂停恢复时 `prepend` 当前句至队首 |
| `lib/features/reader/presentation/widgets/teleprompter_view.dart` | 扫光骨架屏动效、删除旧占位文案 |
| `test/features/reader/teleprompter_view_test.dart` | 更新占位文案断言 |

---

## TTS 暂停完成回调竞态修复（同日追加）

### 根因

`TtsEngineService.playFile()` 会在自然播放完成或 `stopAudio()` 强制释放 `_playCompleter` 后统一调用 `onComplete`。`TtsAudioNotifier.pause()` 原本只依赖瞬时 `_isPausing` 判断暂停中断，若 `onComplete` 晚于 `_isPausing = false` 进入 `_onPlaybackComplete()`，就会误触发 `ReaderProvider.onTtsItemFinished()`，导致暂停后游标切到下一句。

### 修复

- `TtsAudioNotifier` 新增暂停中断哨兵，记录被暂停中断的 `item.id` 与 `session`
- `_onPlaybackComplete()` 命中暂停中断哨兵时仅保留当前进度，不调用 `onTtsItemFinished()`
- `refreshSession()`、`stopAll()`、降级播放前清理暂停哨兵，避免污染新会话
- `ReaderProvider.onTtsItemFinished()` 末句完成时将游标钉到当前完成项，保证完成回调幂等且不越界

### 验证

- `flutter test test/features/audio/tts_audio_notifier_test.dart`
- `flutter test test/features/audio/tts_audio_notifier_test.dart test/features/reader --concurrency=1`
- `flutter analyze`

---

## UI 设计系统交互 Demo（同日追加）

### Demo 目标

基于当前 Flutter 项目的完整形态，先产出可交互的设计系统预览，验证主仪表盘、书架、章节目录、设置、提词器、TTS 控制台与 2048 棋盘的统一视觉方向。

### Demo 核心约束

- Demo 严格映射现有 `CyberColors`、`CyberTextStyles`、`CyberDimensions`、`CyberShadows` 的视觉体系
- Demo 不接入真实业务状态，不读写用户数据，不改变 Flutter 业务代码
- 后续 Flutter 回迁仍以 `core/theme`、`shared/widgets`、`features/*/presentation` 的架构边界为准

### 交付内容

| 文件 | 说明 |
| :-- | :-- |
| `docs/ui-demo/index.html` | 交互式 Demo 页面骨架 |
| `docs/ui-demo/styles.css` | 赛博朋克设计令牌与组件样式 |
| `docs/ui-demo/app.js` | 书架、目录、设置、播放、倍速、棋盘点击等模拟交互 |

### 预览方式

```powershell
python -m http.server 8787 --directory docs/ui-demo
```

浏览器访问：

```text
http://localhost:8787
```

---

## 阅游技能体系增强（同日追加）

### 背景

调研 GitHub 上适合参考的 Agent Skills 来源后，决定采用“吸收结构与检查思路，转译为阅游专属规范”的策略，避免外部通用规则破坏阅游既有架构红线。

### 外部参考方向

- Flutter 官方 Skills：吸收 Widget、集成测试和平台插件测试思路。
- Dart 官方 Skills / `dash_skills`：吸收现代 Dart、纯 Dart domain、异步与测试质量规则。
- `addyosmani/agent-skills`：吸收代码审查、发布门禁、调试恢复和交付验收结构。

### 技能交付内容

| 文件 | 变更 |
| :-- | :-- |
| `.agents/skills/yueyou-test-ci-guard/SKILL.md` | 增强 Flutter 官方测试策略、Dart 测试质量规则和常用回归命令 |
| `.agents/skills/yueyou-code-quality-guard/SKILL.md` | 增加现代 Dart 质量规则，强调模式匹配、空安全、异步 session 守卫和 domain 纯净性 |
| `.agents/skills/yueyou-task-steward/SKILL.md` | 增加与发布就绪守卫的协作边界 |
| `.agents/skills/yueyou-release-readiness-guard/SKILL.md` | 新增发布就绪守卫，固化发版门禁、arm64 APK 打包规范、生产环境注入和拒绝发布条件 |
| `.windsurf/workflows/skill-usage-guide.md` | 更新技能优先级、协作链路、发布打包工作流和决策树 |

### 技能验证

- 文档变更仅涉及 Markdown 和技能规则文件。
- 未修改 Flutter 业务代码，无需运行业务测试。
- README 不更新：本次为内部 Agent 技能体系增强，不改变用户可见功能、启动方式、依赖或核心架构事实。
