# 20260428 测试清理与 CI 覆盖率门控加固

## 一、背景

本日围绕 task.md 优化任务展开，完成以下两项工作：

1. **TTS 引擎异常恢复机制修复**（来自历史遗留任务）
2. **测试体系清理与 CI 覆盖率门控强化**（task.md §2.2）

---

## 二、TTS 引擎异常恢复机制修复

### 问题分析

`TtsEngineService._startPlayLoop` 中，`audioPlayer.setSource` 抛出异常时：

- 捕获了异常，但恢复流程未检查引擎生命周期状态（`_disposed`、`isEnabled`、`_loopSession`）。
- 引擎被禁用/销毁后，异常恢复仍会触发降级逻辑，引发测试时序竞争。

### 解决方案

在 `TimeoutException` 和 `catch(e)` 异常恢复分支中，调用 `_audioPlayer.stop()` 后立即加入生命周期守卫：

```dart
if (_disposed || !isEnabled || _loopSession != sessionAtStep) return;
```

确保异常时正确中断流程，不执行多余熔断与降级通知。

### 涉及文件

- `lib/features/audio/services/tts_engine_service.dart`

---

## 三、测试体系清理（task.md §2.2）

### 3.1 清理临时测试文件

| 文件 | 状态 |
| --- | --- |
| `fix_tests.dart` | ✅ `git rm` 已删除并推送 |
| `fix_tests_new.dart` | ✅ `git rm` 已删除并推送 |
| `fix_tests_v3.dart` | ✅ `git rm` 已删除并推送 |

提交信息：`chore: 彻底清理开发期临时测试辅助文件`

### 3.2 全量测试验证

```text
flutter test --reporter expanded
00:08 +220: All tests passed!
Exit code: 0
```

**220/220 全部通过，零波动失败。**

---

## 四、CI 覆盖率门控加固（task.md §2.2）

### 修改文件

`.github/workflows/flutter-ci.yml`

### 修改内容

| 步骤 | 修改前 | 修改后 |
| --- | --- | --- |
| `flutter analyze` 失败 | 直接 exit，无摘要 | 写入 `GITHUB_STEP_SUMMARY` 中文错误说明后 exit 1 |
| 业务核心覆盖率 < 90% | ⚠️ 仅警告，**不阻断 CI** | ❌ **exit 1 强制失败，禁止合并** |
| 覆盖率达标 | 简单打印 | 输出 `✅ 覆盖率达标` 摘要 |

### 强制阻断逻辑

```yaml
# 覆盖率未达标 → 强制 exit 1
if [ "$PASS" != "yes" ]; then
  echo "## ❌ 覆盖率未达标 — 本次 CI 失败，禁止合并" >> $GITHUB_STEP_SUMMARY
  exit 1
fi
```

> **注意**：GitHub Branch Protection Rules 需在仓库设置中手动开启，
> 将 `Flutter 持续集成 / 分析与测试` 添加为 Required Status Check，
> 才能在 GitHub 层面强制阻止未通过 CI 的 PR 合并。

---

## 五、待办

- [ ] 在 GitHub 仓库 Settings → Branches → Branch protection rules 中，
  为 `yueyou_test` 分支开启 **Require status checks to pass before merging**，
  勾选 `Flutter 持续集成 / 分析与测试`。

---

## 六、补充单元测试覆盖（task.md §2.1）

### 6.1 TileModel 测试（`tile_model_test.dart`）

| 新增用例 | 说明 |
| --- | --- |
| `copyWith 返回新对象` | 验证非同一引用 |
| `连续合并值翻倍` | 模拟 2→4→8→16 合并链路 |
| `id 为 0 时合法` | 边界值构造 |
| `极大 id/value 构造` | 边界值 999999/2048 |
| `value 为 4 时可构造` | 10% 概率分支 |

### 6.2 GameProvider 新增覆盖分支（`game_provider_test.dart`）

| Group | 用例数 | 覆盖分支 |
| --- | --- | --- |
| `eliminateTileById()` | 4 | 正常清除/不存在id/isOver重置/彩蛋音效注入路径 |
| `lastMoveNoMerge` | 2 | 有效移动无合并 true/有合并 false |
| `maxCombo` | 2 | combo超历史最大更新/归零后maxCombo保持 |
| `soundEnabled setter` | 2 | 赋同值不notify/赋不同值notify |
| `存档行数不匹配降级` | 2 | 3行存档/空字符串均降级新局 |
| `flushPersistState()` | 2 | 单次/连续调用不崩溃 |
| `lastMoveDirection` | 2 | 初始null/移动后记录方向 |

### 6.3 全量测试结果

```text
flutter test --reporter expanded
00:09 +241: All tests passed!
Exit code: 0
```

**新增 21 条，总计 241/241 全部通过。**

提交：`test(game_2048): 补充 domain 层与 GameProvider 未覆盖分支的单元测试`

---

## 七、IStorageService 接口抽象（task.md §1.1）

| 文件 | 说明 |
| --- | --- |
| `lib/core/database/i_storage_service.dart` | 抽象接口，34 个方法，100% 中文注释 |
| `lib/core/database/local_storage_service.dart` | 生产实现，委托 StorageService 静态方法 |
| `test/core/i_storage_service_test.dart` | 同套契约测试跑 Mock + Local 两实现，+56 用例 |

**全量测试：297/297 通过（4 跳过）**

提交：`feat(core): 引入 IStorageService 接口抽象与 LocalStorageService 实现`

---

## 八、Sentry 崩溃上报（task.md §3.1-B）

| 文件 | 说明 |
| --- | --- |
| `lib/core/utils/cyber_logger.dart` | 全面重构，分级上报 fatal/error/warning/info |
| `lib/main.dart` | runApp 包裹进 initSentry() 回调 |
| `test/core/cyber_logger_test.dart` | 21 用例，覆盖脱敏/分级/生命周期路径 |

关键设计：DSN 通过 `--dart-define=SENTRY_DSN=...` 注入，隐私合规（attachScreenshot=false / sendDefaultPii=false），开发环境 DSN 为空时静默跳过。

**全量测试：318/318 通过**

提交：`feat(core): 接入 Sentry 崩溃上报并加固 CyberLogger`

---

## 九、环境背景音乐服务（task.md §3.1-C）

| 文件 | 说明 |
| --- | --- |
| `lib/features/audio/services/ambient_service.dart` | Voss-McCartney 粉噪声 + 60Hz 工频嗡鸣代码生成 |
| `lib/main.dart` | 接入初始化 + 生命周期监听 + SettingsProvider 联动 |
| `test/features/audio/ambient_service_test.dart` | 29 用例，WAV 格式校验 + 状态机全路径 |

关键设计：固定随机种子（42）确定性生成，头尾 50ms 淡入淡出无缝循环，AudioContext MEDIA 独立音轨不抢占 TTS。

**全量测试：347/347 通过**

提交：`feat(audio): 实现环境背景音乐服务 AmbientService`
