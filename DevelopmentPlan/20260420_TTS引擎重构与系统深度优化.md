# 阅游 (YueYou) 20260420 任务汇总：TTS引擎重构与系统深度优化

## 项目背景与目标

项目当前处于 V1.0 冲刺阶段。我们需要解决 TTS 引擎异步冲突导致的“双音轨重叠（二重唱）”Bug，同时彻底收敛全 APP 的提示交互（替换为赛博朋克风全局 Toast），并对所有面向用户的报错信息进行“脱敏/世界观翻译”，以符合隐私合规要求。

---

## Task 1: TTS 熔断机制与“二重唱”彻底修复

**文件**：`lib/features/audio/services/tts_engine_service.dart`
**问题根因**：当前降级方案中，`flutter_tts` 发声时，未正确阻塞队列；且预加载的远端音频可能在本地发声时异步触发播放，导致双音轨。

**重构步骤**：

1. **引入降级锁**：在类中新增状态变量 `bool _isDegradedToLocal = false;`。在 `stop()` 和 `init()` 中将其重置为 `false`。
2. **改造 _startPlayLoop (工作流对齐)**：
   - 准备播放前检查 `_isDegradedToLocal`。
   - 若为 `true`，跳过远端，调用 `flutter_tts`。
   - 若为 `false`，尝试远端。若异常则触发熔断：设置 `_isDegradedToLocal = true`，清空队列，触发降级提示，并立即使用本地播放。
3. **修复 Completion Handler 阻塞陷阱**：使用 `Completer<void>` 结合 `_fallbackTts.setCompletionHandler` 严格阻塞循环，直到本地语音读完。
4. **生命周期双重静音**：在 `pause()` 和 `stop()` 中强制同时停止 `_audioPlayer` 和 `_fallbackTts`。

---

## Task 2: 建立全局单例 CyberToast (提示交互收敛)

**目标**：消除项目中散落的 `SnackBar` 和 `showCyberModal`，统一通知规范。

**重构步骤**：

1. **创建组件**：`lib/shared/widgets/cyber_toast.dart` 利用 `Overlay` 实现全局 Toast。
   - **视觉**：屏幕中上部，`blackGlass` 背景，霓虹发光边框。
   - **交互**：滑入淡入，停留 2.5s，单例展示。
2. **全局替换**：移除 `BookshelfProvider` 直接操作 UI 逻辑，替换所有 `SnackBar` 为 `CyberToast`。

---

## Task 3: 隐私合规与异常信息脱敏翻译

**目标**：保护隐私，脱敏报错信息，符合赛博朋克世界观。

**重构步骤**：

1. **重构连接测试**：成功提示“神经网关握手成功”，失败提示“远端神经节点无响应”，杜绝暴露 URL/IP。
2. **重构导入异常**：拦截 IO 异常，统一输出“数据芯片解析失败”，隐藏绝对路径。
3. **全局文案排查**：确保所有提示文案风格统一且无调试信息。
