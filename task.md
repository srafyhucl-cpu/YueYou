YueYou 项目开发侧优化任务（MD格式）

# 一、开发侧优化任务（交付开发，聚焦技术落地）

## 1. 基础架构与代码优化

### 1.1 架构规范化优化（极客标准，追求严谨可扩展）

- [x] 强制引入接口抽象：为核心服务（如StorageService）定义统一接口（IStorageService），接口需包含所有核心方法（存储、读取、删除等），注释清晰，实现类需严格遵循接口规范，提交时需附带接口与实现类的单元测试，实现依赖倒置，解耦业务逻辑与具体实现，杜绝紧耦合代码，确保AI开发时可精准对接接口规范
- [x] 状态管理升级规划：启动Riverpod迁移适配，逐步替换现有Provider，需提供完整的迁移方案（含模块迁移顺序、兼容处理、测试用例），利用其类型安全、依赖管理灵活的优势，解决多模块联动场景下的状态交互问题，提升代码健壮性，确保AI可按方案有序迁移，无兼容性风险

## 2. 测试体系完善

### 2.1 补充测试覆盖度

| 测试类型 | 优先级 | 覆盖范围 | 状态 |
| --- | --- | --- | --- |
| 单元测试 | 🔴 高 | domain 层纯业务逻辑（TextParser、TileModel） | ✅ 已完成 |
| Widget 测试 | 🟡 中 | 核心 UI 组件（TileWidget、TeleprompterView） | ✅ 已完成 |
| 集成测试 | 🟡 中 | 核心流程（听书 / 游戏合并） | ✅ 已完成（game_flow_integration_test / reader_flow_integration_test）|

### 2.2 解决测试遗留问题

- [x] 清理临时文件：彻底删除fix_tests.dart/fix_tests_new.dart/fix_tests_v3.dart临时文件，删除后需执行flutter test确认测试无异常，提交时需附带测试通过截图/日志，确保AI可验证清理结果
- [x] 测试稳定性保障：所有415项测试用例需100%稳定通过，无波动失败案例，需优化波动测试用例（如异步测试添加超时控制、依赖模拟完善），确保AI可复现测试结果
- [x] CI校验配置：配置CI完整校验逻辑（flutter analyze + flutter test --coverage），测试失败时强制阻止代码合并，生成测试覆盖率报告，配置测试失败告警，明确CI配置文件修改细节，确保AI可精准配置CI流程

## 3. 功能开发与落地

### 3.1 完成开发中功能

| 功能 | 优先级 | 开发要求（适配AI开发，指令明确可落地） |
| --- | --- | --- |
| 环境背景音乐播放器 | 🟡 中 | 基于现有ambientEnabled/ambientVol字段，接入播放后端，实现开关控制、音量调节、暂停/继续联动（与TTS朗读联动），添加异常处理（播放失败提示、日志上报），提交时附带单元测试与功能测试用例 |
| 崩溃上报（Sentry/Crashlytics） | 🔴 高 | 基于现有CyberLogger钩子，接入崩溃上报工具，实现异常日志（堆栈信息、设备信息、操作路径）收集、分级上报（严重/普通异常区分），添加日志脱敏处理，确保上报信息合规，提交时附带上报测试用例（模拟异常上报） |
| 热更新版本检查 | 🟢 低 | 基于DashboardScreen现有存根，实现版本对比（本地版本与服务器版本）、更新提示（强制/可选更新区分）、更新链接跳转，添加异常处理（接口请求失败兜底），提交时附带版本对比测试用例 |

## 4. 基础性能优化

- [x] TTS缓冲优化：保持默认6句缓冲，需实现缓冲队列监控逻辑，实时反馈缓冲状态，出现缓冲不足时自动触发预加载，同时添加异常处理（缓冲失败重试、日志上报），确保AI开发时可精准实现流畅播放逻辑
- [x] 动画性能优化：低配设备动画简化需实现设备性能检测逻辑（CPU、内存占用检测），根据检测结果自动切换动画等级（高配完整特效、低配基础动画），需明确动画等级划分标准，确保AI可按标准实现自适应逻辑
- [x] TTS缓存管理：实现缓存清理策略（按缓存大小+缓存时间双重判定），定期后台执行清理任务，添加清理日志，同时预留缓存清理手动触发接口，明确清理阈值（如缓存超过500MB自动清理），确保AI开发时可精准落地缓存管理逻辑

## 5. 开发文档与协作规范

### 5.1 补充架构文档（极客规范，完整可追溯）

- 核心流程文档：详细梳理TTS朗读、书籍导入、游戏逻辑等关键流程，使用PlantUML绘制时序图，标注关键节点、异常分支、数据流向，每个节点需附带详细注释（含输入输出参数、处理逻辑），确保AI可通过时序图精准理解业务逻辑，无歧义
- 模块依赖文档：补充模块依赖关系图，明确各模块（core/features/shared）的依赖边界、核心接口与实现类的关联，标注接口调用规范（参数要求、返回值格式、异常处理），确保AI开发时可严格遵循依赖规范，避免跨模块调用异常

### 5.2 完善协作规范（极客标准，严谨统一）

- 代码提交规范：严格遵循Conventional Commits规范，明确提交类型（feat/fix/docs/chore等）、提交信息格式（类型: 描述，可选详情），提供10+典型提交示例，确保AI提交代码时可严格遵循规范，提交记录清晰可追溯
- 分支管理策略：严格遵循Git Flow流程，明确各分支（master/dev/feature/hotfix）的创建规范、合并条件、删除时机，标注分支命名规则（如feature/tts-optimize），确保AI开发时可按规范管理分支，避免分支混乱
- 贡献指南完善：撰写完整的CONTRIBUTING.md，明确代码规范（命名、注释、代码结构）、提交规范、分支规范、测试规范、AI开发注意事项（如接口适配、异常处理要求），确保AI可按指南规范开发，适配后续团队协作与代码交接
