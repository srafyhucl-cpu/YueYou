# 阅游手机原型合同检查

## 检查范围

本记录只检查 `docs/product/20260713_阅游产品改进手机原型.html` 的静态结构合同，
不把 HTML 结构通过当作浏览器点击、Flutter Widget、Android 真机或商业支付证据。

执行命令：

```powershell
python scripts/test_product_prototype.py
python scripts/check_product_prototype.py --json
```

## 当前结果

- [x] 390 x 844 视口合同：`width=device-width`、`initial-scale=1`、390px 宽、844px 高。
- [x] 六个演示场景：继续听读、首次进入、异常恢复、荣誉解锁、付费对比、完本时刻。
- [x] 三根导航：听读、书架、陪伴。
- [x] 三个陪伴分段：Xiaoyo、印记、活动。
- [x] 两个书境分段：基础书境、主题书境。
- [x] 异常重试/本地音色、全文、对比、完本和下一本关键节点存在并绑定脚本事件。
- [x] 21 个直接按钮事件和 7 个委托事件均通过静态绑定合同检查，覆盖导航、场景、书籍、
  播放、全文、对比、异常恢复、完本、搜索和排序路径。
- [x] 五个本地 Xiaoyo 候选图引用均能解析到仓库文件，无远程资源依赖。
- [x] 原型脚本拒绝网络/支付 API，且明确播放、Rive、荣誉和支付均为模拟反馈，未发现真实
  网络支付入口。

检查输出：`ok=true`，`errors=[]`，`warnings=[]`，场景 6、导航 3、陪伴分段 3、书境分段 2、
图片 5。Python 单测：`4 passed / 0 failed`。

## 未覆盖范围

Python 标准库检查器没有执行真实浏览器点击、390 x 844 截图像素检查、键盘焦点链、
移动端文件选择器、Android/iOS 真机或支付行为；这些仍需对应运行环境和负责人证据。
