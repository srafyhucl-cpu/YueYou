# 阅游 v1.1.2 发布说明

## 版本信息

| 项目 | 值 |
| --- | --- |
| 版本号 | `1.1.2+4` |
| Git Tag | `v1.1.2` |
| Tag 目标提交 | 待发布提交创建后回填 |
| APK 类型 | Android arm64-v8a Release APK |
| APK 产物 | `build/app/outputs/flutter-apk/app-arm64-v8a-release.apk` |
| APK SHA-256 | GitHub production artifact 生成后回填 |

## 本版重点

- 收口后续治理提交的发布一致性：版本号、Sentry release、APK versionName/versionCode、Tag 和变更记录统一为 `1.1.2`。
- 保留 `v1.1.1` 历史 Tag 与 artifact，不覆盖既有发布证据。
- 延续听读主线、隐私前置、TTS 安全和 CI 门禁治理成果。

## 发布前检查

- [x] `pubspec.yaml` 版本为 `1.1.2+4`。
- [x] `CyberLogger` 默认 release 为 `yueyou@1.1.2`。
- [x] README 构建示例使用 `APP_VERSION=1.1.2`。
- [ ] 当前治理提交已推送到 `origin/yueyou_test`。
- [ ] `v1.1.2` Tag 已创建并推送，且指向当前发布提交。
- [ ] GitHub Actions `release-apk` 成功完成。
- [ ] GitHub artifact 中 APK 的 versionName/versionCode 为 `1.1.2/4`。
- [ ] GitHub artifact SHA-256 已回填。

## 验证记录

发布提交创建后回填 `flutter analyze`、测试、覆盖率、AI 门禁、Go 门禁、GitHub Run、Tag 目标提交和 APK SHA-256。

## 回滚说明

保留 `v1.1.1` 与 `v1.1.2` 的发布证据。如 `v1.1.2` 出现阻断问题，停止分发该版本并按服务端回滚流程恢复，不删除或覆盖历史 Tag。
