# 阅游 v1.1.1 发布说明

## 版本信息

| 项目 | 值 |
| --- | --- |
| 版本号 | `1.1.1+3` |
| Git Tag | `v1.1.1` |
| Tag 目标提交 | `fa981d2f73d4a4c14dae1a009b3635608c58e259`（Tag 保持不变） |
| APK 类型 | Android arm64-v8a Release APK |
| APK 产物 | `build/app/outputs/flutter-apk/app-arm64-v8a-release.apk` |
| APK SHA-256 | 本地正式签名构建：`603857D462DC3C24C163B0A077010D4FB1DC8288DDE298CD05AD919F7DA423AB`；GitHub artifact：`a9fd40c35ce526b92f2a9c0910e673df1738b14041249a0bf449e6f543ba6f4e` |

## 本版重点

- 清理历史泄密风险：重写 Git 历史，移除旧 Android 签名密码和本机 JKS 路径。
- 启动隐私闸门：未同意隐私协议前不初始化完整业务栈。
- TTS 安全治理：服务端不记录朗读原文，TTS 临时音频走私有对象和短效签名 URL。
- Android 本地数据边界：禁用系统自动备份和 Android 12+ 数据提取。
- 测试与 CI 门禁：Flutter 分析、测试目录分析、AI 工程门禁、覆盖率、Go 服务端测试和 race 检查进入 CI。
- 发布治理：新增手动 arm64 Release APK 构建任务，版本、Sentry release 和 APK versionName/versionCode 同源。

## 发布前检查

- [x] `pubspec.yaml` 版本为 `1.1.1+3`。
- [x] `CyberLogger` 默认 release 为 `yueyou@1.1.1`。
- [x] `v1.1.1` Tag 已推送，且解析到发布提交
  `fa981d2f73d4a4c14dae1a009b3635608c58e259`。
- [x] `origin/main`、`origin/develop`、`origin/yueyou_test` 已同步到当前治理修复提交
  `3c5351a` 已包含于三个远端分支；`v1.1.1` Tag 保留原发布提交。
- [x] GitHub 默认分支已切换为 `main`，并启用 `分析与测试` 必需检查、至少 1 个 PR 审批、禁止强推和删除。
- [x] GitHub `production` environment 已配置 `ANDROID_KEYSTORE_BASE64`、`ANDROID_STORE_PASSWORD`、`ANDROID_KEY_ALIAS`、`ANDROID_KEY_PASSWORD`。
- [x] GitHub Actions `release-apk` 手动任务成功完成（Run `29190744467`）。
- [x] 当前提交本地正式签名 APK SHA-256 已记录：
  `603857D462DC3C24C163B0A077010D4FB1DC8288DDE298CD05AD919F7DA423AB`。
- [x] GitHub artifact SHA-256 已写回本文件：
  `a9fd40c35ce526b92f2a9c0910e673df1738b14041249a0bf449e6f543ba6f4e`；artifact ID 为 `8259540304`。

> 注意：`v1.1.1` Tag 保持既有发布提交，当前 GitHub artifact 属于后续治理提交，二者尚未形成
> 同一 Tag、提交、APK 哈希和版本号闭环。后续应递增版本并重新生成正式发布 Tag，不覆盖既有 Tag。
> 后续闭环：`v1.1.2+4` 和 `v1.1.2` Tag 已完成收口；本文件保留为
> `v1.1.1` 历史发布证据，不修改其 Tag 或 artifact。

## 验证记录

- `flutter analyze`：通过，0 issue。
- `flutter test test\core\cyber_logger_test.dart --concurrency=1`：通过，26/26。
- `dart scripts\ai_code_checker.dart`：通过，0 阻断、0 warning。
- 当前提交本地正式签名构建：`flutter build apk --release --target-platform android-arm64
  --split-per-abi` 成功；APK 通过 v2 签名校验，大小 `25,856,776` 字节。
- `.github/workflows/flutter-ci.yml`：YAML 可解析，包含 `release-apk` job。

## 回滚说明

如 `v1.1.1` 发布后发现阻断问题：

1. 在应用市场停止继续分发 `v1.1.1` APK。
2. 保留 `v1.1.1` Tag 与 artifact，不删除发布证据。
3. 从 `main` 建立修复分支，完成修复后递增到 `1.1.2+4`，禁止覆盖 `v1.1.1`。
4. 若必须撤回服务端能力，优先回滚 `/www/wwwroot/yueyou/yueyou-server` 二进制并重启 `yueyou.service`。
5. 如 TTS 对象访问或签名 URL 策略异常，先关闭或收紧服务端 TTS 入口，再补发客户端修复版。

## 已知外部依赖

- GitHub 默认分支、分支保护、production Secrets 和 release artifact 已完成外部验收；artifact
  SHA-256 为 `a9fd40c35ce526b92f2a9c0910e673df1738b14041249a0bf449e6f543ba6f4e`。
- `go test -race ./...` 本机缺少可用的 64 位 GCC 无法执行，已由 Ubuntu CI Run `29190150172`
  门禁验证通过。
- 本工作树未包含本地 `android/key.properties` 或 JKS，正式 APK 必须由 GitHub production Secrets 构建。
