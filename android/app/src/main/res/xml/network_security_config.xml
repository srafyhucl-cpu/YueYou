<?xml version="1.0" encoding="utf-8"?>
<!--
  阅游 网络安全配置（NetworkSecurityConfig）

  P0-7：删除 AndroidManifest 中的 usesCleartextTraffic="true"，
  仅允许本地调试地址（localhost / 127.0.0.1 / 10.0.2.2 模拟器）使用明文 HTTP，
  其余所有域名强制 HTTPS，符合 TapTap / 应用市场安全审核要求。
-->
<network-security-config>
    <!-- 默认全局策略：仅允许 HTTPS，拒绝任何明文 -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- 调试白名单：本地开发环境允许明文 HTTP -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
