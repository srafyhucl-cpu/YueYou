# Android 签名配置模板

# 本文件仅用于说明格式。真实签名配置必须放在未跟踪的 android/key.properties 中，
# 或通过 CI Secret 注入 ANDROID_STORE_FILE、ANDROID_STORE_PASSWORD、
# ANDROID_KEY_ALIAS、ANDROID_KEY_PASSWORD。
# 禁止提交 key.properties、JKS、Keystore 或任何真实密码。

storeFile=release.jks
storePassword=替换为本机或 CI Secret 中的密码
keyAlias=release
keyPassword=替换为本机或 CI Secret 中的密码
